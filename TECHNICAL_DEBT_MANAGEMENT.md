# مدیریت بدهی فنی و برنامه توسعه فرانت‌اند اکو نوژین
## چارچوب جامع مدیریت بدهی فنی و برنامه‌ریزی توسعه

| مورد | جزئیات |
|---|---|
| **تاریخ** | ۱۳ سپتامبر ۲۰۲۶ |
| **پروژه** | Eco Nojin / HyDroMa Frontend |
| **نسخه** | 1.0 |
| **وضعیت** | آغاز — برای اجرای تیم فنی |

---

## فهرست مطالب
1. [تعریف‌ها و طبقه‌بندی](#۱-تعریف‌ها)
2. [روش‌شناسی ارزیابی](#۲-روش‌شناسی)
3. [راهبردهای اصلاح](#۳-راهبردها)
4. [تعادل سرعت و کیفیت](#۴-تعادل)
5. [مؤشرات کلیدی عملکرد](#۵-kpi)
6. [نقشه راه نمونه](#۶-نقشه-راه)

---

## ۱. تعریف‌ها و طبقه‌بندی بدهی فنی

### ۱.۱. تعریف بدهی فنی

بدهی فنی (Technical Debt) هزینه‌ی ناشی از انتخاب راه‌حل‌های ساده‌تر در حال حاضر است که باید در آینده بازپرداخت شود. در فرانت‌اند اکو نوژین بدهی فنی به ۵ دسته تقسیم می‌شود:

| نوع | تعریف | مثال در پروژه |
|---|---|---|
| **بدهی کد** | کد نپیدا، تکراری، یا بدون تست | ۳۰+ مورد `lang === 'fa'` تکراری |
| **بدهی معماری** | قرارگیری زیر سطح فنی مطلوب | SPA خالص بدون SSR/SSG |
| **بدهی امنیتی** | آسیب‌پذیری‌های امنیتی | `.env` با توکن‌های واقعی |
| **بدهی مستندات** | مستندات ناکافی یا قدیمی | برنامه‌های متعارض |
| **بدهی وابستگی** | وابستگی‌های قدیمی یا بی‌مصرف | `@tailwindcss/vite@4.1.11` — پیشرفته |

### ۱.۲. طبقه‌بندی اولویت

| اولویت | تعریف | معیار تصمیم‌گیری | زمان‌بندی |
|---|---|---|---|
| **P0 — بحرانی** | اجتناب‌ناپذیر برای عملکرد یا امنیت | خطر ریکوردینگ، نقض امنیت، یا توقف سرویس | ۰ تا ۲ هفته |
| **P1 — بالا** | تأثیر مستقیم بر UX یا کیفیت کد | کاهش قابل توجه بهره‌وری توسعه یا کیفیت UX | ۱ تا ۴ هفته |
| **P2 — متوسط** | ارتقای قابلیت‌ها و رقابت‌پذیری | بهبود قابل اندازه‌گیری امن غیراکید | ۱ تا ۳ ماه |
| **P3 — بلندمدت** | تمایز استراتژیک و گسترش بازار | منطبق با اهداف ۱۲+ ماهه | ۳ تا ۱۲ ماه |

### ۱.۳. ماتریس ارزیابی شدت

هر مورد بدهی فنی بر اساس ۴ محور امتیاز‌بندی می‌شود (هر کدام ۱ تا ۵):

```
امتیاز کل = (تأثیر × احتمال × هزینه‌ی بازپرداخت) / زمان‌بندی

تأثیر ۱-5:  از بدون تأثیر تا بحرانی
احتمال ۱-5:  از بعید تا قطعی
هزینه ۱-5:  از کم تا بسیار زیاد
زمان‌بندی ۱-5: از فوری تا دور
```

---

## ۲. روش‌شناسی ارزیابی

### ۲.۱. فرآیند شناسایی

```
┌─────────────────┐
│  اسکن خودکار    │──→  ابزارها: ESLint, TypeScript, pnpm audit, Lighthouse
└────────┬────────┘
         ▼
┌─────────────────┐
│  بازبینی دستی   │──→  هر ۲ هفته توسط تیم فنی
└────────┬────────┘
         ▼
┌─────────────────┐
│  ثبت در داشبورد │──→  فایل TECHNICAL_DEBT.md
└────────┬────────┘
         ▼
┌─────────────────┐
│  اولویت‌بندی    │──→→ بر اساس ماتریس ۱.۲
└─────────────────┘
```

### ۲.۲. ابزارهای شناسایی

| ابزار | حوزه | فرکانس |
|---|---|---|
| `npx tsc --noEmit` | نوع‌ها | هر commit |
| `npx eslint src/` | کیفیت کد | هر commit |
| `npx vitest run` | تست‌ها | هر commit |
| `pnpm audit` | آسیب‌پذیری وابستگی | هفته‌ای |
| `npx vite build` | ساخت | هر برانچ |
| Lighthouse CI | عملکرد | هر PR |
| `secrets_audit.py` | نشت اطلاعات | pre-commit |
| Bundle Visualizer | اندازه باندل | هر فاز |

### ۲.۳. مستندسازی بدهی

هر مورد بدهی فنی ثبت‌شده باید شامل موارد زیر باشد:

```yaml
بدهی فنی:
  شناسه: TD-YYYY-NNN
  عنوان: توضیح مختصر
  نوع: [کد/معماری/امنیتی/مستندات/وابستگی]
  اولویت: [P0/P1/P2/P3]
  امتیاز: [1-5 هر کدام]
  فایل‌های مربوطه: [لیست مسیرها]
  وضعیت: [باز/در حال رفع/حل شده]
  مالک: [نام تیم یا فرد]
  تاریخ شناسایی: YYYY-MM-DD
  تاریخ حل: YYYY-MM-DD (اختیاری)
  پیوند برنامه: [پیوند به برنامه مرتبط]
```

---

## ۳. راهبردهای اصلاح

### ۳.۱. اصل ۱: پرداخت نقد (Pay Down)

برای بدهی‌های P0: توقف توسعه جدید و اختصاص ۱۰۰٪ منابع به رفع.

**موارد فعلی P0:**
- `.env` با توکن‌های واقعی — محافظت و ایمن‌سازی
- تناقض تنظیمات `useBilingual` بین `src/lib/i18n.ts` و `src/hooks/useBilingual.ts`
- جدول‌های CSS تکراری (`@animate-shimmer` dup)

### ۳.۲. اصل ۲: پوشش با تست (Coverage)

برای بدهی‌های P1: ایجاد تست‌های یکپارچه قبل از ریفکتورینگ.

**هدف:** پوشش تست بالای ۴۰٪ برای کد داشبورد و کتابخانه‌های کلیدی.

### ۳.۳. اصل ۳: مهاجرت تدريجی (Strangler Fig)

برای بدهی‌های معماری P1-P2: مهاجرت تدریجی بدون بازنویسی کامل.

**مثال:** مهاجرت از SPA به Astro SSR:
```
فاز ۱: صفحات عمومی → Astro (SSR/SSG)
فاز ۲: داشبورد → باقی‌مانده SPA با hydration
فاز ۳: بازیابی مسیر → React Router 7 با data APIs
```

### ۳.۴. اصل ۴: انکار بدهی (Denial)

هرگز! هرگز نگذارید بدهی فنی پنهان بماند.

**سیاست:** هر PR باید بدهی فنی جدید ایجاد نکند مگر با تأیید صریح تیم فنی.

### ۳.۵. اصل ۵: کاهش فعال (Active Reduction)

**۲۰٪ زمان هر اسپرینت** اختصاص داده شود به رفع بدهی فنی انباشته‌شده.

### ۳.۶. اصل ۶: اتوماسیون (Automation)

```yaml
pre-commit:
  - secrets_audit.py        # پیشگیری از نشت اطلاعات
  - tsc --noEmit              # ایجاد یکپارچگی نوع
  - eslint src/               # کیفیت کد

CI/CD:
  - vitest run                # اجرای تست‌ها
  - lighthouse CI             # عملکرد
  - bundle visualizer         # اندازه باندل
  - pnpm audit                # آسیب‌پذیری
```

---

## ۴. تعادل سرعت و کیفیت

### ۴.۱. معادله تعادل

```
سرعت = تعداد story points در اسپرینت
کیفیت = (۱ - نرخ باگ) × پوشش تست × امتیاز لایسنس

تعادل = سرعت × کیفیت / بدهی فنی انباشته
```

### ۴.۲. قوانین تعادل

| قانون | شرح |
|---|---|
| **قانون ۱** | اگر بدهی فنی > ۳۰٪ ظرفیت، توقف توسعه و رفع |
| **قانون ۲** | هر ۵ story point توسعه = ۱ story point بدهی فنی |
| **قانون ۳** | هر PR باید حداقل ۱ تست یا ۱ نوع ایمنی داشته باشد |
| **قانون ۴** | بازبینی کد باید قبل از merge انجام شود (حداقل ۱ reviewer) |
| **قانون ۵** | هر ۴ هفته بازبینی کامل بدهی فنی |

### ۴.۳. مدل سرعت پایدار

```
سرعت هدف = میانگین ۳ اسپرینت گذشته (نه حداکثر)

اگر سرعت واقعی > ۱.۲ × سرعت هدف ← خطر فرسودگی
اگر سرعت واقعی < ۰.۸ × سرعت هدف ← خطر رکود
```

### ۴.۴. بودجه بدهی فنی در برنامه‌ریزی

| نوع برنامه | بودجه بدهی فنی |
|---|---|
| اسپرینت عادی | ۲۰٪ |
| اسپرینت بحرانی | ۵۰٪ |
| فاز توسعه جدید | ۱۰٪ (پایه‌سازی) |
| برنامه‌ریزی بلندمدت | ۳۰٪ |

---

## ۵. مؤشرات کلیدی عملکرد (KPIs)

### ۵.۱. KPIs فنی

| متریک | فعلی (تخمینی) | هدف ۳ ماهه | هدف ۶ ماهه | هدف ۱۲ ماهه |
|---|---|---|---|---|
| **Lighthouse Performance** | ~40-50 | >70 | >85 | >95 |
| **Lighthouse Accessibility** | ~60 | >75 | >85 | >95 |
| **Lighthouse SEO** | ~70 | >85 | >95 | >95 |
| **FCP** | ~3.5s | <2s | <1.5s | <0.8s |
| **LCP** | ~5s | <2.5s | <1.5s | <1s |
| **CLS** | ~0.1 | <0.05 | <0.01 | <0.01 |
| **Bundle Size (main)** | TBD | <3MB | <2MB | <1.5MB |
| **TypeScript Errors** | ۰ | ۰ | ۰ | ۰ |
| **ESLint Errors** | ۰ | ۰ | ۰ | ۰ |
| **Test Coverage** | ~5% | >20% | >40% | >60% |
| **pnpm audit (critical)** | TBD | ۰ | ۰ | ۰ |
| **Secret Leaks** | ۱ (`.env`) | ۰ | ۰ | ۰ |

### ۵.۲. KPIs کسب‌وکاری

| متریک | هدف ۶ ماهه | هدف ۱۲ ماهه | هدف ۱۸ ماهه |
|---|---|---|---|
| **Active Users (MAU)** | ۱,۰۰۰ | ۱۰,۰۰۰ | ۵۰,۰۰۰ |
| **Farmers Onboarded** | ۵۰۰ | ۵,۰۰۰ | ۱۵,۰۰۰ |
| **Pilot Projects** | ۱۰ | ۵۰ | ۱۰۰ |
| **Uptime** | ۹۹.۵% | ۹۹.۹% | ۹۹.۹۵% |
| **API Response Time** | <500ms | <200ms | <150ms |

### ۵.۳. KPIs بدهی فنی

| متریک | هدف ماهانه |
|---|---|
| **نسبة بدهی فنی حل‌شده** | >20٪ از کل بدهی انباشته |
| **نرخ تولید بدهی فنی جدید** | <5٪ ظرفیت توسعه |
| **زمان میانگین حل بدهی P0** | <48 ساعت |
| **پوشش تست کد داشبورد** | >40٪ در ۳ ماه |
| **فرسودگی کد** | بازبینی هر ۶ ماه |

### ۵.۴. داشبورد ردیابی

پیشنهاد ایجاد یک صفحه داشبورد داخله (یا استفاده از Linear/Jira) با ویجت‌های زیر:
- **کل بدهی فنی** (تعداد و امتیاز)
- **به‌روزرسانی‌های ۷ روز گذشته**
- **P0 آتی‌ها**
- **نمودار روند** (عملکرد / کیفیت / سرعت)

---

## ۶. نقشه راه نمونه

### ۶.۱. فاز ۰ — فوری (۰ تا ۲ هفته) — ایمن‌سازی و پایه‌سازی

| # | وظیفه | نوع | زمان | مالک |
|---|---|---|---|---|
| 0.1 | `.env` ایمن‌سازی و `.env.example` | امنیتی | ۱ روز | TBD |
| 0.2 | Anti-leak hook تقویت | امنیتی | ۱ روز | TBD |
| 0.3 | حذف `shimmer` تکراری CSS | کد | ۱ ساعت | TBD |
| 0.4 | اصلاح `ProjectProperties` در Map | کد | ۲ ساعت | TBD |
| 0.5 | `vite-env.d.ts` — اعلان متغیرهای محیطی | کد | ۱ ساعت | TBD |
| 0.6 | یکسان‌سازی `useBilingual` | کد | ۲ روز | TBD |

### ۶.۲. فاز ۱ — پایه (۱ تا ۴ هفته) — کیفیت کد

| # | وظیفه | نوع | زمان |
|---|---|---|---|
| 1.1 | پوشش تست‌های واحد برای lib تابعی | تست | ۱ هفته |
| 1.2 | EmptyState UI component | کد | ۳ روز |
| 1.3 | Skip Navigation (a11y) | دسترسی‌پذیری | ۱ روز |
| 1.4 | دستیار `useBilingual` یکپارچه | کد | ۲ روز |
| 1.5 | Prettier یکپارچه و اجرا | ابزار | ۱ روز |
| 1.6 | ESLint strict config | کیفیت | ۱ روز |

### ۶.۳. فاز ۲ — بهبود (۱ تا ۳ ماه) — عملکرد

| # | وظیفه | نوع | زمان |
|---|---|---|---|
| 2.1 | SSR/SSG — مهاجرت صفحات عمومی به Astro | معماری | ۳ هفته |
| 2.2 | PWA/Service Worker | عملکرد | ۲ هفته |
| 2.3 | Bundle Optimization + Visualizer | عملکرد | ۱ هفته |
| 2.4 | Critical CSS Inlining | عملکرد | ۱ هفته |
| 2.5 | MapLibre با NDVI/NDWI real tiles | ویژگی | ۳ هفته |
| 2.6 | Chart Library (Recharts/Chart.js) | ویژگی | ۲ هفته |

### ۶.۴. فاز ۳ — ارتقا (۳ تا ۶ ماه) — فناوری

| # | وظیفه | نوع | زمان |
|---|---|---|---|
| 3.1 | React 19 Migration | فناوری | ۴ هفته |
| 3.2 | TanStack Query — مدیریت داده | معماری | ۲ هفته |
| 3.3 | Real-time Pipeline (WebSocket/SSE) | ویژگی | ۳ هفته |
| 3.4 | WebSocket/Kafka Integration | معماری | ۲ هفته |

### ۶.۵. فاز ۴ — توسعه (۶ تا ۱۲ ماه) — ویژگی‌های جدید

| # | وظیفه | نوع | زمان |
|---|---|---|---|
| 4.1 | AI Advisory Engine | هوش مصنوعی | ۸ هفته |
| 4.2 | Mobile App (React Native) | موبایل | ۱۲ هفته |
| 4.3 | Carbon Marketplace | بازار | ۱۰ هفته |
| 4.4 | Digital Twin (3D) | ویژگی | ۱۶ هفته |
| 4.5 | IoT Dashboard | داشبورد | ۶ هفته |

### ۶.۶. فاز ۵ — استراتژی (۱۲ تا ۱۸ ماه) — گسترش

| # | وظیفه | نوع | زمان |
|---|---|---|---|
| 5.1 | Open API Marketplace | پلتفرم | ۱۲ هفته |
| 5.2 | Academic Research Portal | پلتفرم | ۸ هفته |
| 5.3 | ReFi Protocol | مالی | ۱۶ هفته |
| 5.4 | Global Expansion (30+ language) | بین‌المللی | ۱۲ هفته |

---

## ضمیمه: چک‌لیست اجرایی

### هر اسپرینت:
- [ ] بدهی فنی جدید شناسایی و ثبت شد
- [ ] بودجه ۲۰٪ بدهی فنی اختصاص داده شد
- [ ] تست‌ها سبز هستند
- [ ] TypeScript strict pass
- [ ] ESLint zero errors
- [ ] Lighthouse score > هدف فاز
- [ ] pnpm audit zero critical

### هر ماه:
- [ ] بازبینی کامل بدهی فنی
- [ ] به‌روزرسانی داشبورد بدهی فنی
- [ ] رفع تمام P0 آتی‌ها
- [ ] بازبینی وابستگی‌ها

### هر فاز:
- [ ] ارزیابی کامل عملکرد با Lighthouse CI
- [ ] بازبینی معماری
- [ ] بازنگری نقشه راه
- [ ] Security audit خارجی (۶ ماهه)

---

*این چارچوب بر اساس تحلیل کد منبع و برنامه‌های موجود تهیه شده و باید با تصمیم تیم فنی بازبینی و تأیید شود.*
