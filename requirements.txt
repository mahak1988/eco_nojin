# ============================================================================
# Eco Nojin / HyDroMa — pinned runtime requirements
# Derived from tested environment (Python 3.11, Windows + Linux)
# Policy (docs/12_30_year_strategy): compatible-release bounds, never open-ended
# Last updated: 2026-08-18 (Phase 0 - GIS Stack added)
# ============================================================================

# === Core API ===
fastapi>=0.141,<1.0
uvicorn[standard]>=0.52,<1.0
pydantic>=2.13,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.2,<2.0
python-multipart>=0.0.32,<1.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt==4.0.1  # pinned: bcrypt>=4.1 breaks passlib 1.7.4 (no __about__, >72-byte ValueError)

# === Database (Current: SQLite, Phase 1: PostgreSQL + PostGIS) ===
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
asyncpg>=0.29,<1.0        # Async PostgreSQL driver (Phase 1)
psycopg2-binary>=2.9,<3.0 # Sync PostgreSQL driver (fallback)

# === Scientific stack (verified against numba 0.67 / numpy 2.4) ===
numpy>=2.4,<3.0
numba>=0.67,<1.0
pandas>=3.0,<4.0
scipy>=1.17,<2.0
scikit-learn>=1.5,<2.0

# === GIS & Remote Sensing Stack (NEW - Phase 0) ===
# Pinned to versions verified compatible with numpy 2.4 / Python 3.11
geopandas>=1.0,<2.0
rasterio>=1.3.10,<1.4
rioxarray>=0.15,<1.0
xarray>=2024.7,<2025.0
shapely>=2.0,<3.0
pyproj>=3.6,<4.0
fiona>=1.9,<2.0
pystac-client>=0.8,<1.0
planetary-computer>=1.0,<2.0
osmnx>=1.9,<2.0

# === Big Data & Storage (Phase 0+) ===
dask[complete]>=2024.8,<2025.0
zarr>=2.18,<3.0
netCDF4>=1.7,<2.0
h5py>=3.11,<4.0
duckdb>=1.0,<2.0          # Already used in services/analytics

# === HTTP & Integrations ===
requests>=2.34,<3.0
httpx>=0.28,<1.0

# === Testing ===
pytest>=9.0,<10.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=4.1,<5.0
pytest-timeout>=2.4,<3.0
httpx>=0.28,<1.0           # for TestClient

# === Phase 1: Telegram + Multi-platform bots ===
aiogram>=3.30,<4
# Pin aiohttp explicitly: python-bale-bot downgrades it and breaks aiogram
aiohttp==3.14.3

# === Security & Audit (optional but recommended) ===
bandit>=1.7,<2.0
pip-audit>=2.7,<3.0

# === Quality (dev-only, install via: pip install -r requirements-dev.txt) ===
# ruff>=0.2,<1.0
# mypy>=1.8,<2.0
# radon>=6.0,<7.0
# === Blockchain (simulated ledger / Phase 8) ===
# web3 is installed; eth-tester provides the in-process test EVM.
# NOTE: eth-tester[py-evm] pulls safe-pysha3 (sdist-only C ext). If the network
# cannot reach files.pythonhosted.org, install it separately or skip blockchain
# tests (web3_provider imports it lazily so the rest of the suite still runs).
web3>=7.0,<8.0
eth-tester[py-evm]>=0.12,<1.0

# === Added during prerequisite reconciliation (2026-08-18) ===
cdsapi>=0.7,<1.0        # ERA5 / CDS climate data client (services/satellite/cds.py)
h5netcdf>=1.7,<2.0      # xarray NetCDF engine used by tests/test_era5.py
