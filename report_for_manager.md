# گزارش ارزیابی وضعیت فنی-حقوقی اکو نوژین — اکوکوین و اعتبار کربن

> **نوع سند:** فقط تحلیل است؛ بدون تغییر در کد یا سایت. هر یافته با مرجع کدیزیابی شده است.

---

## ۱. خلاصهٔ اجرایی

اکو نوژین ترکیبی از یک موتور علمی MRV واقعی (RothC/ERA5/SoilGrids/KoBo)، کیف‌پول دیجیتال EcoWallet DB-backed با تقسیم ۷۰/۱۵/۱۰/۵، و یک شبیه‌ساز بلاکچین درون‌حافظه‌ای دارد که تراکنش‌های آن با هش UUID ساختگی تولید می‌شوند. قرارداد `CarbonCredit.sol` (ERC20 با role/mint/burn/pause) وجود دارد اما اسکریپت‌ deploy آن را منتشر نمی‌کند. رجیستری رسمی، VVB مستقل و مکانیسم جلوگیری از احتساب دوگانه هنوز پیاده نشده‌اند. در عوض، فرانت‌اِند برخی ادعاها را (ثبت روی «پالیگون»، استانداردهای «Verra / Puro-Earth / ISO 14064-2») قوی‌تر از واقعیت کد بیان می‌کند. خلاصه: زیرساخت علمی مهیا نیست، اما گواهی یا عرضهٔ معتبر هنوز نداریم.

---

## ۲. مقایسهٔ اکوکوین با اعتبار کربن

| ویژگی | اکوکوین (هم‌اکنون) | اعتبار کربن استانداردی (Verra/Puro/Gold Standard) |
|---|---|---|
| **منبع ارزش** | پاداش اقدام واقعی؛ امروز توکن کاربردی | گواهی کاهش انتشار معتبر |
| **برابری** | ارواح اعتبار کربنی را در آینده می‌تواند داشته باشد | ۱ تن CO₂e معادل یک تک کردد |
| **صدور** | ۱۰۰٪ داخل‌پلتفرمی؛ فروش ثانویه نه | صادر توسط رجیستری معتبر (Verra، Puro، Gold Standard) |
| **تساوی اعتبار** | بر پایهٔ MRV درونی (RothC + KoBo) | وابسته به VVB مستقل + ISO 14064-2 |
| **قابلیت انتقال** | درون‌شبکه؛ هنوز بلاکچین زنده نیست | روی بلاکچین یا رجیستری متمرکز قابل انتقال |
| **عدم احتساب دوگانه** | در حال ساخت (پیشنهاد G2) | الزامی در رجیستری؛ ماده ۶ پاریس |
| **پخش درآمد** | ۷۰/۱۵/۱۰/۵ واضح در `distribution.py` | درصد متفاوت؛ بسته به طرح |
| **پشتوانهٔ قانونی** | توکن کاربردی (LEGAL_COMPLIANCE.md)؛ نه ارز دیجیتال قابل معامله | مستلزم بررسی قانون ارز دیجیتال هر کشور |

> نتیجهٔ مقایسه: اکوکوین هنوز «اعتبار کربن» نیست؛ فقط زیرساخت تقلید همان را دارد.

---

## ۳. امتیاز بلوغ هر لایه (۰ تا ۵)

| لایه | وضعیت فعلی | امتیاز | شواهد |
|---|---|---|---|
| **MRV علمی** | حلقهٔ محاسبه واقعی؛ RothC/ERA5/SoilGrids + KoBo | **۴** | `carbon_mrv.py` وضعیت `modelled_estimate` / `field_verified` شفاف |
| **گواهی‌نامه / VVB** | وجود ندارد | **۰** | `carbon_mrv.py` صراحتاً «NOT a certification» می‌گوید |
| **رجیستری بلاکچین** | شبیه‌ساز درون‌حافظه | **۱** | `carbon_registry.py`: `dict` + `uuid.uuid4().hex` برای tx hash |
| **قرارداد هوشمند** | `CarbonCredit.sol` هم‌تراز کامل؛ ولی deploy نشده | **۲** | `contracts/scripts/deploy.js` ۵ قرارداد دیگر را deploy می‌کند، CarbonCredit نه |
| **کیف‌پول (EcoWallet)** | DB-backed Earn/Redeem، ۷۰/۱۵/۱۰/۵ | **۳** | `ledger.py`, `distribution.py`, `test_ecowallet.py` |
| **جلوگیری از دوگانگی** | در طرح؛ هنوز کد نیست | **۱** | `ecocoin.ts` زنجیرهٔ ۵گانه شامل «ثبت در رجیستری» می‌شود اما رجیستری فعال نیست |
| **فرانت‌اِند** | ادعاها قوی‌تر از کد | **۲** | `site.ts` «Verra / Puro-Earth / ISO 14064-2» می‌گوید؛ واقعاً شبیه‌ساز است |

---

## ۴. ریسک‌های F0 و F1

### F1 — فنی (قابل رفع با برنامه‌ریزی)
1. **`CarbonCredit.sol` deploy نشده است** — اسکریپت `deploy.js` ۵ قرارداد دیگر را منتشر می‌کند اما CarbonCredit را نادیده می‌گیرد؛ بنابراین هیچ توکن ERC20 واقعی روی زنجیره نیست.
2. **رجیستری هنوز شبیه‌ساز است** — `carbon_registry.py` از `dict` و `uuid` برای tx hash استفاده می‌کند؛ هش‌ها ساختگی‌اند (`0x{uuid.uuid4().hex[:64]}`) و پس از ریست شدن سرور از دست می‌روند.
3. **عدم احتساب دوگانه درون‌ساخت نشده** — هیچ مکانیزم «corresponding adjustment» یا مهارت مرکزی شناسه وجود ندارد.
4. **VVB مستقل وجود ندارد** — هیچ سرویس‌گیرمحور یا API برای اعتبارسنجی توسط نهاد ثالثی نیست.

### F0 — حقوقی / برند (فوری و بحرانی)
1. **اختلاف اکوکوین**: بیانیهٔ رسمی «اکوکوین = اعتبار کربن معادل ۱ تن CO2، قابل عرضه» می‌گوید اما `LEGAL_COMPLIANCE.md` و `ecocoin.ts` تأکید می‌کنند اکوکوین توکن کاربردی است؛ انتشار بیانیهٔ کنونی می‌تواند آن را از «توکن امن» به «ابزار سرمایه‌گذاری» تبدیل کند.
2. **ادعای بلاکچین پالیگون در فرانت‌اِند**: `site.ts` و `carbon.astro` می‌گویند «ثبت روی بلاکچین پالیگون» اما هیچ قراردادی مستقر نشده است و رجیستری فقط شبیه‌ساز است؛ این شباهت سبزشویی است.
3. **استانداردهای Verra/Puro در فرانت‌اِند**: `site.ts` و `impactcarbon.ts` «Verra / Puro-Earth / ISO 14064-2» را به‌عنوان استاندارد ثبت می‌کنند؛ در حالی که MRV فقط «simplified accounting» است و گواهی ندارد.
4. **امضاء و تاریخ بیانیه**: نام «شرکت کشت و صنعت دشت امید نارون» در کد/ریادمیت وجود ندارد و تاریخ پایانی «۲۰۲۵» از بین رفته است.

---

## ۵. نقشهٔ اقدام ۳۰/۶۰/۹۰ روزه

| بازه | اقدام | مسئولیت |
|---|---|---|
| **۳۰ روز** | الف) حذف/اصلاح ادعای «ثبت روی پالیگون» از فرانت‌اِند؛ جایگزین شوند «ثبت در رجیستری داخلی (شبیه‌ساز)»<br>ج) حذف ادعای «Verra/Puro-Earth» به‌عنوان استاندارت ثبت‌شده؛ فقط «مرجع محاسبه» باشد<br>د) اضافه کردن برچسب «simplified / not certified» به تمام متون MRV | تیم فرانت‌اِند + علمی |
| **۶۰ روز** | الف) افزودن CarbonCredit.sol به `deploy.js` یا حذفش از repo اگر قصد استقرار نداریم<br>ج) پیاده‌سازی مکانیزم شناسهٔ یکتا + جلوگیری از احتساب دوگانه در `carbon_registry.py`<br>ه‍) تهیهٔ چارچوب VVB مستقل (لیست سازمان‌های یا شرکت مشاوره) | تیم بلاکچین + MRV |
| **۹۰ روز** | الف) صدور نظر حقوقی مستقل در هر حوزهٔ قضایی (G3)<br>ج) به‌روزرسانی کامل ToS ماده ۳.۲ (قابل مسدودسازی بودن مطابق AML/FATF)<br>ه‍) ثبت/تأیید شخصیت حقوقی «دشت امید نارون» و اصلاح نام در فوتر | حقوقی + برند |

---

## ۶. Claimهای مجاز و ممنوع

### ✅ مجاز
- «MRV ما با موتور هیدروما + RothC/ERA5/SoilGrids محاسبه می‌شود؛ وضعیت برآورد `modelled_estimate` یا `field_verified` واضح است.»
- «EcoWallet یک کیف‌پول DB-backed است با عملکرد Earn/Redeem؛ هر پرداختی بر اساس قانون ۷۰/۱۵/۱۰/۵ تقسیم می‌شود.»
- «CarbonCredit.sol یک قرارداد ERC20 کامل با نقش‌های mint/burn/pause است؛ هنوز منتشر نشده است.»
- «رجیستری کربن در حال حاضر یک شبیه‌ساز درون‌حافظه است؛ هش‌های تراکنش ساختگی‌اند.»
- «اکوکوین در فاز ۱ یک توکن کاربردی است؛ اقدام می‌شود نه خرده‌فروشی یا سرمایه‌گذاری.»
- «MRV «simplified accounting» بر اساس Verra VM0032 / Gold Standard است؛ گواهی نیست مگر VVB مستقل باشد.»

### ❌ ممنوع
- «اعتبار کربن اکوکوین معادل دقیق ۱ تن CO2e است» (بدون VVB).
- «اعتبارها روی بلاکچین پالیگون ثبت می‌شوند» (قرارداد deploy نشده است).
- «استاندارد ما Verra / Puro-Earth است» (هیچ گواهی یا پروژهٔ ثبت‌شده در این رجیستری‌ها نیست).
- «اکوکوین قابل معامله / عرضه در بازار ثانویه است».
- «بدون رضایت قابل مسدودسازی نیست» (مناقطع با FATF).
- «حداقل ۵۰٪ به جوامع محلی می‌رسد» (هنوز عددی در `distribution.py` تعریف نشده است؛ ۷۰/۱۵/۱۰/۵ صرفاً تقسیم هزینه است).
- هر ادعایی که بچرخد «تأییدشده توسط بلاکچین پالیگون» یا «مرجع استاندارد بین‌المللی» باشد.

---

## ۷. بخش حقوقی — نیازمند نظر رسمی

> **وضعیت:** این بخش حقوقی است و تحت بررسی حقوقی قرار دارد. هیچ ادعای قطعی در مورد رأی قضایی، مجوز یا سازگاری قوانین ندارد.

1. **اکوکوین به‌عنوان توکن کاربردی:** بر اساس `LEGAL_COMPLIANCE.md` (آزمون Howey)، اکوکوین در فاز ۱ از نظر آمریکایی «توکن کاربردی نیست؛ اوراق بهادار محسوب نمی‌شود». با این حال، انتشار بیانیهٔ رسمی که «اکوکوین = اعتبار قابل عرضه» می‌گوید، ریسک بازطبقه‌بندی به اوراق بهادار دارد (MiCA/EU، SEC/USA).

2. **رجیستری و استانداردها:** هیچ ارتباطی با Verra، Puro-Earth یا Gold Standard ثبت نشده است. استفاده از نام این نهادها در فرانت‌اِند صرفاً «مرجع محاسبه» بوده و نه گواهی.

3. **قفل‌گذاری AML/FATF:** تعهد «قابل مسدودسازی نیست» در ماده ۳.۲ بیانیه، مستلزم اصلاح فوری است؛ FATF و پاریس‌نشینی‌ها الزام توقیف قانونی بر اساس دستور قضایی را دارند.

4. **جوانب حقوقی در ایران:** بر اساس LEGAL_COMPLIANCE.md، «ماینینگ مجاز، معامله محدود»؛ اکوکوین تا زمانی که به‌عنوان «اعتبار خدماتی درون‌پلتفرمی» باقی بماند، کم‌ریسک است؛ انتشار در بازار ثانویه نیازمند مجوز بانک مرکزی است.

5. **گام بعدی:** صدور نظر حقوقی مستقل (G3) در هر حوزهٔ قضایی هدف؛ پس از آن، هر عرضه یا فروش بیرونی تنها بعد از تأیید حقوقی اعلام می‌شود.

---

*گزارش تهیه شده بر اساس بررسی مستقیم کدهای موجود در تاریخ ۲۰۲۶-۰۹-۱۶؛ هیچ فایلی در مخزن تغییر نداشته است.*