/**
 * PostCSS config — Tailwind CSS v4 via its official PostCSS plugin.
 * (Phase 0 removed Tailwind as unused; Phase 3 reintroduces it as v4.)
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
