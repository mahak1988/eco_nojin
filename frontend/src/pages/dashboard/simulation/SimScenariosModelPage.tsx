import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: sim-scenarios (category: simulation). */
export default function SimScenariosModelPage() {
  return <ModelDetail modelId="sim-scenarios" />;
}
