import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: weap (category: simulation). */
export default function WeapModelPage() {
  return <ModelDetail modelId="weap" />;
}
