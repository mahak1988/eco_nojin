import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: sim-calibration (category: simulation). */
export default function SimCalibrationModelPage() {
  return <ModelDetail modelId="sim-calibration" />;
}
