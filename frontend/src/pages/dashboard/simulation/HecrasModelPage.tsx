import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: hecras (category: simulation). */
export default function HecrasModelPage() {
  return <ModelDetail modelId="hecras" />;
}
