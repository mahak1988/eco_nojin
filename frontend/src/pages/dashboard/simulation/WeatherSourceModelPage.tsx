import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: weather-source (category: simulation). */
export default function WeatherSourceModelPage() {
  return <ModelDetail modelId="weather-source" />;
}
