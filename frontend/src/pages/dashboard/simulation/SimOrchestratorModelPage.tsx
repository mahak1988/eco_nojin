import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: sim-orchestrator (category: simulation). */
export default function SimOrchestratorModelPage() {
  return <ModelDetail modelId="sim-orchestrator" />;
}
