import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: hpheno (category: indices). */
export default function HphenoModelPage() {
  return <ModelDetail modelId="hpheno" />;
}
