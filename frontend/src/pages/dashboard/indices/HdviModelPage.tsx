import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: hdvi (category: indices). */
export default function HdviModelPage() {
  return <ModelDetail modelId="hdvi" />;
}
