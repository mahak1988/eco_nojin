import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: hyrue (category: indices). */
export default function HyrueModelPage() {
  return <ModelDetail modelId="hyrue" />;
}
