import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: hlhs (category: indices). */
export default function HlhsModelPage() {
  return <ModelDetail modelId="hlhs" />;
}
