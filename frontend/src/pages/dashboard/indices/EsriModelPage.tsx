import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: esri (category: indices). */
export default function EsriModelPage() {
  return <ModelDetail modelId="esri" />;
}
