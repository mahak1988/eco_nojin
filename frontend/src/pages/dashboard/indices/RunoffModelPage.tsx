import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: runoff-model (category: indices). */
export default function RunoffModelModelPage() {
  return <ModelDetail modelId="runoff-model" />;
}
