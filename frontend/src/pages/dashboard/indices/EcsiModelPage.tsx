import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: ecsi (category: indices). */
export default function EcsiModelPage() {
  return <ModelDetail modelId="ecsi" />;
}
