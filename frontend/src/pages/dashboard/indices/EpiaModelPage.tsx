import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: epia (category: indices). */
export default function EpiaModelPage() {
  return <ModelDetail modelId="epia" />;
}
