import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-water-retention (category: soil). */
export default function SoilWaterRetentionModelPage() {
  return <ModelDetail modelId="soil-water-retention" />;
}
