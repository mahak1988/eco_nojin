import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: pedotransfer (category: soil). */
export default function PedotransferModelPage() {
  return <ModelDetail modelId="pedotransfer" />;
}
