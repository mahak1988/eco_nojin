import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-health (category: soil). */
export default function SoilHealthModelPage() {
  return <ModelDetail modelId="soil-health" />;
}
