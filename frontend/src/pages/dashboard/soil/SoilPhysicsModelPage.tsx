import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-physics (category: soil). */
export default function SoilPhysicsModelPage() {
  return <ModelDetail modelId="soil-physics" />;
}
