import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-salinity (category: soil). */
export default function SoilSalinityModelPage() {
  return <ModelDetail modelId="soil-salinity" />;
}
