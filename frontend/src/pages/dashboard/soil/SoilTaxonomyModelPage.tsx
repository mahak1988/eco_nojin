import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-taxonomy (category: soil). */
export default function SoilTaxonomyModelPage() {
  return <ModelDetail modelId="soil-taxonomy" />;
}
