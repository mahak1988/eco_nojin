import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-recommendations (category: soil). */
export default function SoilRecommendationsModelPage() {
  return <ModelDetail modelId="soil-recommendations" />;
}
