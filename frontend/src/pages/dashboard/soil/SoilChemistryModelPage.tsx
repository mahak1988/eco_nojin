import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-chemistry (category: soil). */
export default function SoilChemistryModelPage() {
  return <ModelDetail modelId="soil-chemistry" />;
}
