import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: soil-texture (category: soil). */
export default function SoilTextureModelPage() {
  return <ModelDetail modelId="soil-texture" />;
}
