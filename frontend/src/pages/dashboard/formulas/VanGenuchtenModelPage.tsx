import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: van-genuchten (category: formulas). */
export default function VanGenuchtenModelPage() {
  return <ModelDetail modelId="van-genuchten" />;
}
