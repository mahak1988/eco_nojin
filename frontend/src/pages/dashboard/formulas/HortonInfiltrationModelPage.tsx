import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: horton-infiltration (category: formulas). */
export default function HortonInfiltrationModelPage() {
  return <ModelDetail modelId="horton-infiltration" />;
}
