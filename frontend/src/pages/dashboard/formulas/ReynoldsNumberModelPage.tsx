import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: reynolds-number (category: formulas). */
export default function ReynoldsNumberModelPage() {
  return <ModelDetail modelId="reynolds-number" />;
}
