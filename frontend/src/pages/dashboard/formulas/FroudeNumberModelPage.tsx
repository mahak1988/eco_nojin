import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: froude-number (category: formulas). */
export default function FroudeNumberModelPage() {
  return <ModelDetail modelId="froude-number" />;
}
