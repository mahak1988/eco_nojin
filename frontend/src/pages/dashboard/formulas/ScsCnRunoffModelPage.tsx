import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: scs-cn-runoff (category: formulas). */
export default function ScsCnRunoffModelPage() {
  return <ModelDetail modelId="scs-cn-runoff" />;
}
