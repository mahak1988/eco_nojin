import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: ring-storage (category: formulas). */
export default function RingStorageModelPage() {
  return <ModelDetail modelId="ring-storage" />;
}
