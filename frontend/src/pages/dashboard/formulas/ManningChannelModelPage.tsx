import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: manning-channel (category: formulas). */
export default function ManningChannelModelPage() {
  return <ModelDetail modelId="manning-channel" />;
}
