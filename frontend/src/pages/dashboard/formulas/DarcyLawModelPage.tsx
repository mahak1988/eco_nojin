import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: darcy-law (category: formulas). */
export default function DarcyLawModelPage() {
  return <ModelDetail modelId="darcy-law" />;
}
