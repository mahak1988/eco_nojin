import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: groundwater-model (category: hydro). */
export default function GroundwaterModelModelPage() {
  return <ModelDetail modelId="groundwater-model" />;
}
