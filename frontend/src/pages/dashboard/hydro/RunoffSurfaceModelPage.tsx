import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: runoff-surface (category: hydro). */
export default function RunoffSurfaceModelPage() {
  return <ModelDetail modelId="runoff-surface" />;
}
