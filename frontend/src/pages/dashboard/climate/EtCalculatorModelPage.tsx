import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: et-calculator (category: climate). */
export default function EtCalculatorModelPage() {
  return <ModelDetail modelId="et-calculator" />;
}
