import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: irrigation-scheduler (category: climate). */
export default function IrrigationSchedulerModelPage() {
  return <ModelDetail modelId="irrigation-scheduler" />;
}
