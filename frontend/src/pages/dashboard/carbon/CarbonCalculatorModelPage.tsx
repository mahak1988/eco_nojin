import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: carbon-calculator (category: carbon). */
export default function CarbonCalculatorModelPage() {
  return <ModelDetail modelId="carbon-calculator" />;
}
