import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: mrv-satellite (category: carbon). */
export default function MrvSatelliteModelPage() {
  return <ModelDetail modelId="mrv-satellite" />;
}
