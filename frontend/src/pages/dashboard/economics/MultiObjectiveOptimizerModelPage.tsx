import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: multi-objective-optimizer (category: economics). */
export default function MultiObjectiveOptimizerModelPage() {
  return <ModelDetail modelId="multi-objective-optimizer" />;
}
