import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-roi (category: economics). */
export default function EconRoiModelPage() {
  return <ModelDetail modelId="econ-roi" />;
}
