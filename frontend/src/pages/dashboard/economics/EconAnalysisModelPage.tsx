import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-analysis (category: economics). */
export default function EconAnalysisModelPage() {
  return <ModelDetail modelId="econ-analysis" />;
}
