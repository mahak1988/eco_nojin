import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-costing (category: economics). */
export default function EconCostingModelPage() {
  return <ModelDetail modelId="econ-costing" />;
}
