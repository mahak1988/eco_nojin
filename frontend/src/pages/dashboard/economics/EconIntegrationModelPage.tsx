import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-integration (category: economics). */
export default function EconIntegrationModelPage() {
  return <ModelDetail modelId="econ-integration" />;
}
