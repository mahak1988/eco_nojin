import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-risk (category: economics). */
export default function EconRiskModelPage() {
  return <ModelDetail modelId="econ-risk" />;
}
