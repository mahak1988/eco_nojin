import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: econ-revenue (category: economics). */
export default function EconRevenueModelPage() {
  return <ModelDetail modelId="econ-revenue" />;
}
