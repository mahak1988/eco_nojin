import ModelDetail from '../../../components/dashboard/ModelDetail';

/** HyDroMa dashboard - model page: decision-support (category: economics). */
export default function DecisionSupportModelPage() {
  return <ModelDetail modelId="decision-support" />;
}
