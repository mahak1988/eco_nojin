# Disable build scripts to avoid sharp compilation issues
# We use images.unoptimized in next.config.js instead
enable-pre-post-scripts=false
ignore-scripts=true

# pnpm settings
shamefully-hoist=true
strict-peer-dependencies=false
