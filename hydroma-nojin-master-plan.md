# برنامهٔ جامع پلتفرم «هیدروما نوژین» — سند معماری، طراحی و فناوری (نسخهٔ ۲٫۱ — بازبینی واقعیت و نقشهٔ توسعه ۲۰۲۶)

**نسخه:** ۲٫۱ · **تاریخ:** ۱۴۰۴/۰۷/۰۱ — بازبینی و به‌روزرسانی ۲۰۲۶/۰۹/۲۲
**دامنه:** مطالعهٔ کد و اسناد پروژه، بازبینی وضعیت واقعی repository، معماری اطلاعات، فهرست صفحات، راهنمای طراحی هر صفحه، پشتهٔ فناوری فرانتاند ۲۰۲۶، برنامهٔ ۱۴ زبانه، استانداردها، امنیت، داده، هوش مصنوعی، زیرساخت و فازبندی
**جایگاه سند:** این نسخه جایگزین نسخهٔ ۲٫۰ است و آن را با شواهد کد فعلی، گزارش‌های ممیزی و فناوری‌های پایدار ۲۰۲۶ کامل می‌کند. نسخهٔ ۱٫۰ و ۲٫۰ به‌صورت کامل در پیوست پروژه نگهداری می‌شوند.
**وضعیت زبان:** متن مرجع فارسی است؛ نسخهٔ انگلیسی این سند پس از تأیید تولید می‌شود (`docs/en/00_master_plan.md` همگام خواهد شد).
**قانون اعتبار سند:** هرجا ادعای این سند با کد، `HEAD`، خروجی تست یا وضعیت working tree ناسازگار باشد، شواهد اجرایی مقدم است و اختلاف باید در رجیستر ضعف ثبت شود.

---

## خلاصهٔ تغییرات نسبت به نسخهٔ ۱٫۰

| # | تغییر | خلاصه |
|---|---|---|
| ۱ | مطالعهٔ واقعیت پروژه | شناخت کامل مونوریپو، موتور علمی، ۵۷ سرویس، سه نسل فرانتاند و وضعیت i18n به سند اضافه شد |
| ۲ | تثبیت ۱۴ زبان رسمی | فهرست رسمی زبانها با کد موجود پروژه هم‌راستا شد (fa، en، ar، ur، de، es، fr، hi، it، ms، pt، ru، zh، bn) و tr/ps به موج دوم منتقل شد |
| ۳ | پشتهٔ فناوری ۲۰۲۶ | معماری مرجع فرانتاند بر پایهٔ جدیدترین وضعیت اکوسیستم (React 19 RSC، Next.js App Router، Tailwind CSS 4، CSS مدرن، PWA آفلاین‌محور) تعریف شد |
| ۴ | نظام طراحی «زمینِ زنده» | جهت طراحی منحصربه‌فرد، توکنها، تایپوگرافی چنداسکریپتی و ۱۲ قالب صفحه تعریف شد |
| ۵ | راهنمای طراحی هر صفحه | برای هر بافت و هر گروه صفحه، راهنمای طراحی ملموس نوشته شد؛ برای صفحه‌های شاخص، شرح بخش‌به‌بخش |
| ۶ | صفحات جدید نسخهٔ ۲ | ۶۸ صفحه/قالب جدید در ۶ کانون (هوش مصنوعی، اعتماد و منشأ داده، توسعه‌دهندگان، کیف پول و مالی، آفلاین، منطقه‌ای) اضافه شد: 917 → 985 |
| ۷ | فازبندی اصلاح شد | فاز ۰ به «یکسان‌سازی پشته + زیرساخت i18n» اختصاص یافت؛ همهٔ فازها i18n-اول شدند |

---

## بخش ۰ — اصول حاکم بر کل پلتفرم

| اصل | توضیح عملی |
|---|---|
| زبان پیش‌فرض فارسی | همهٔ صفحات ۱۴زبانه‌پذیر از روز اول؛ کلیدهای ترجمه جدا از کد؛ هیچ متن hard-code در کامپوننت‌ها |
| دسترسی عمومی بدون ثبت‌نام | صفحات عمومی و بازارگاه (مرور و مشاهده) بدون ورود قابل استفاده‌اند |
| دادهٔ واقعی یا برچسب صریح | هر عدد یا منبع دارد یا «برآورد مدل» / «نیازمند تنظیم» برچسب میخورد؛ «مُهر منبع» روی هر داده |
| سلسله‌مراتب نظارتی سه‌لایه | فروشگاه → بازارچه → بازارگاه؛ نظارت از پایین به بالا تسری می‌یابد |
| قواعد سخت‌گیرانهٔ تجارت | پذیرش قرارداد نسخه‌دار پیش از هر کنش مالی |
| تفکیک کامل پنج بافت | هر بافت زبان بصری مشترک اما تراکم، ناوبری و ابزار خودش را دارد |
| یک پشتهٔ واحد | پایان دادن به تکثر فریمورک؛ یک معماری مرجع برای هر پنج بافت |
| طراحی توکن‌محور | هیچ رنگ/فونت/فاصلهٔ خام در کد؛ همه از توکنهای W3C DTCG تولید میشود |
| عملکرد به‌مثابه ویژگی | بودجهٔ عملکرد اجباری: LCP≤2.5s، INP≤200ms، CLS≤0.1؛ اسکریپت صفحات عمومی ≤200KB gzip |
| دسترس‌پذیری اجباری | WCAG 2.2 AA برای همهٔ بافتها؛ کیبورد-اول؛ `prefers-reduced-motion` |
| وب سبز | بهینه‌سازی تصویر/فونت/درخواست؛ میزبانی کم‌کربن؛ رسانهٔ تنبل — سازگار با مأموریت اکولوژیک پلتفرم |

### فهرست رسمی ۱۴ زبان پلتفرم

این فهرست با مستندات پروژه (`docs/en/07_i18n_localization.md`) و کد موجود هم‌راستاست و جایگزین فهرست ۶+۶ نسخهٔ ۱٫۰ میشود.

| ردیف | زبان | کد | جهت | موج عرضه | فونت رابط | یادداشت |
|---|---|---|---|---|---|---|
| ۱ | فارسی | `fa` | RTL | موج ۰ (پیش‌فرض) | Vazirmatn | مرجع محتوا و زبان fallback همهٔ زبانها |
| ۲ | انگلیسی | `en` | LTR | موج ۰ | Vazirmatn (لاتین) | زبان فنی، مستندات، API |
| ۳ | عربی | `ar` | RTL | موج ۰ | Vazirmatn + Noto Sans Arabic | بازار منطقه‌ای؛ تفاوت واژگان با fa |
| ۴ | اردو | `ur` | RTL | موج ۰ | Noto Nastaliq UI (سرصفحه) / Noto Sans Arabic (متن) | پاکستان؛ خط خاص نیازمند line-height بیشتر |
| ۵ | آلمانی | `de` | LTR | موج ۱ | Vazirmatn (لاتین) | نهادهای اروپایی؛ واژگان کشاورزی دقیق |
| ۶ | اسپانیایی | `es` | LTR | موج ۱ | Vazirmatn (لاتین) | آمریکای لاتین — بزرگ‌ترین پیادهٔ گسترش |
| ۷ | فرانسوی | `fr` | LTR | موج ۱ | Vazirmatn (لاتین) | آفریقای فرانکوفون، FAO |
| ۸ | پرتغالی | `pt` | LTR | موج ۱ | Vazirmatn (لاتین) | برزیل؛ تنوع خطابهٔ کشاورزی |
| ۹ | هندی | `hi` | LTR | موج ۱ | Noto Sans Devanagari | اعداد هندی، جمع‌بندی پیچیدهٔ ICU |
| ۱۰ | چینی | `zh` | LTR | موج ۱ | Noto Sans SC | بازار نهاده و تجهیزات؛ بدون خط فاصله، شکستن خط CJK |
| ۱۱ | روسی | `ru` | LTR | موج ۲ | Vazirmatn (سیریلیک) | آسیای مرکزی و قفقاز |
| ۱۲ | اندونزیایی/مالایو | `ms` | LTR | موج ۲ | Vazirmatn (لاتین) | اندونزی/مالزی؛ جنگل و روغن‌نخل |
| ۱۳ | ایتالیایی | `it` | LTR | موج ۲ | Vazirmatn (لاتین) | زراعت مدیترانه‌ای |
| ۱۴ | بنگالی | `bn` | LTR | موج ۲ | Noto Sans Bengali | بنگلادش — متراکم‌ترین دلتای کشاورزی |

**زبانهای موج توسعهٔ منطقه‌ای (خارج از ۱۴ اصلی، پس از راهاندازی منطقه‌ای):** ترکی `tr` ( Turkey/آذربایجان)، پشتو `ps` (افغانستان)، پنجابی، سندی و هائوسا بر پایهٔ دادهٔ تقاضا.

**قواعد فنی چندزبانه (اجباری):**
1. یک منبع واحد کلیدها: `messages/<locale>/*.json` با فضانام به تفکیک بافت (`public.*`، `market.*`، `science.*`، `admin.*`، `research.*`، `system.*`).
2. مذاکرهٔ زبان در middleware سرور: کوکی → `Accept-Language` → پیش‌فرض `fa`؛ مسیر همیشه با پیشوند زبان (`/fa/…`، `/en/…`).
3. fallback زنجیرهای: `locale → en → fa`؛ هر رشتهٔ برگشتیِ ترجمه‌نشده برچسب «ترجمهٔ ماشینی» میگیرد و در پنل ادمین صف میشود.
4. قالب‌بندی با ICU MessageFormat (جمع، جنسیت، انتخاب) و `Intl.NumberFormat/DateTimeFormat` محلی؛ اعداد علمی همیشه لاتین با `JetBrains Mono`، حتی در RTL.
5. `hreflang` + `lang`/`dir` پویا روی `<html>`؛ نقشهٔ سایت به تفکیک زبان؛ URL زبان‌محور قابل اشتراک.
6. شبه‌محلی‌سازی (pseudo-localization) در CI برای کشف برش متن و overflow.
7. برچسب کیفیت ترجمه: «انسانی تأییدشده» / «ماشینی + بازبینی» / «ماشینی»؛ محتوای توصیهٔ کشاورزی هرگز «ماشینی خالص» نمیماند.

---

## بخش ۱ — مطالعهٔ پروژه (مبنای واقعیت)

### ۱.۱ نمای کلان مونوریپو `D:\eco_nojin`

| مسیر | نقش | وضعیت |
|---|---|---|
| `engine/hydroma/` | موتور علمی پایتونی — ۴۰+ زیرماژول (خاک، اقلیم، هیدرولوژی، فرسایش، کربن، MRV، ماهواره، سناریو، اقتصاد، آبیاری، تصمیم‌یار، بهینه‌سازی، AI کمکی، مولد، جذر پایتون-C++) | فعال |
| `engine/cpp_core/` | هستهٔ عددی C++20 (Richards، Saint-Venant، FAO-56، RUSLE، نمونه‌برداری MC/LHS) با pybind11 | پایدار؛ ۷۰/۷۰ تست سبز |
| `services/` | ۵۷ سرویس میکروسرویسی (auth، ledger، marketplace، mrv، satellite، map_engine، dispute_resolution، provenance، ecowallet، event_bus، jobs، observability و…) | فعال؛ ۱۲۸/۱۲۸ تست |
| `frontend/` | وبسایت/اپ عمومی — **در شاخهٔ main حذف شده** و نسخهٔ کامل آن در worktreeهای `.kilo/worktrees/*` نگهداری میشود | نیازمند تصمیم بازیابی |
| `mobile/` | اپ موبایل React Native/Expo با معماری آفلاین‌اول | پایه |
| `packages/` | `api-client`، `config`، `types`، `ui` | پایه |
| `blockchain/`، `contracts/`، `foundry/` | رجیستری کربن و زنجیرهٔ تأمین روی بلاکچین | پایه |
| `docs/` | ADRها، معماری (شامل `local_first` و پروتکل sync)، مستندات دوزبانهٔ en/fa | غنی |
| `supabase/`، `alembic/`، `database/` | Postgres+RLS، مهاجرتها | فعال |
| `deploy/`، `helm/`، `k8s/`، `render.yaml`، `railway.toml` | استقرار چندسکویی | فعال |

### ۱.۲ سرویسهای بکاند و پیوند با صفحات

۵۷ سرویس موجود مستقیماً قابلیت نگاشت به بافتهای پنج گانه را دارند:

| بافت | سرویسهای پشتیبان |
|---|---|
| عمومی | content، notification، bots (telegram)، quality_assurance |
| بازارگاه | marketplace، commerce، inventory، logistics، finance، ledger، ecowallet، dispute_resolution، quality، provenance، contracts |
| علمی هیدروما | models، scientific_motors، simulation، mrv، satellite، map_engine، ogc، science، data، data_sources، validation، oracle |
| ادمین | admin، audit، security، privacy، observability， jobs، event_bus، workers， reliability، backup |
| پژوهش | science، data_manual، ecosystem، integration |
| مشترک | api_gateway، auth， analytics， reporting， ai، field_monitoring، mobile_monitoring， tourism، landscape, land， livestock |

> هر لایهٔ پنهان موتور (نسخهٔ مدل، کالیبراسیون، عدم‌قطعیت، حسابرسی علمی) در سرویسهای بالا وجود دارد؛ وظیفهٔ فرانتاند آشکارسازی آن‌ها در UI است، نه بازسازی آن‌ها.

### ۱.۳ فرانتاند موجود (سه نسل)

| نسل | پشته | وضعیت | درس |
|---|---|---|---|
| نسل ۱ (بایگانی) | Next.js + react-i18next؛ ۱۴ فایل locale (en, fa, ar, de, es, fr, hi, it, ms, pt, ru, ur, zh, bn)؛ RTL ناقص | در بایگانی `EcoJin_Legacy_Archive_2026` | فهرست ۱۴ زبان و کلیدها دارایی ارزشمند قابل بازیابی است |
| نسل ۲ (worktree فعال) | Vite 6 + React 19 + Astro 4 (SSR هیبرید) + Tailwind 4 + React Router 7 + TanStack Query + framer-motion + lenis + maplibre + recharts؛ ۴۰۵ فایل؛ ~۴۵ صفحهٔ عمومی + ۶۱ صفحهٔ مدل علمی + ۱۷ صفحهٔ بازارگاه + ۹ صفحهٔ Live + محتوای تایپ‌شدهٔ fa/en | کامل‌ترین نسخه؛ در `main` ادغام نشده | معماری محتوا (`content/sections/*` + `content/pages/*`) عالی است و باید حفظ شود |
| نسل ۳ (README) | Vite 8 + React 19 + antd 6 + deck.gl 9 + three.js + zustand + react-i18next 17 | ادعای README؛ پیاده‌سازی ناهمگون | توهم مستندات؛ باید یک پشتهٔ مرجع مستند شود |

**داراییهای قابل حفظ از نسل ۲:** تقسیم محتوا به ۲۳ فایل تایپ‌شده، `LanguageContext` با RTL، کامپوننتهای حالت (Skeleton/EmptyState/ErrorBoundary/Toast)، توکنهای پالت «شب‌جنگلی/سبز»، فونتهای باندل‌شدهٔ محلی (Vazirmatn، Markazi Text، JetBrains Mono)، sitemap/robots/OG، تستهای Playwright و accessibility.

### ۱.۴ وضعیت i18n واقعی

| لایه | وضعیت |
|---|---|
| کلیدها | نسل ۱: ۱۴ زبان × ۹۵ کلید کامل؛ نسل ۲: محتوای fa/en تایپ‌شده (structure-based نه key-based)؛ نسل ۳: react-i18next |
| RTL | fa/ur/ps در نسل ۲ (از طریق `dir`)، `ar` ناقص؛ CSS منطقی نیمه‌بهره‌برداری شده |
| مسیریابی زبانی | پیشوند `/fa/…` پیاده نشده؛ زبان فقط state کلاینت است — سئوی چندزبانه ندارد |
| اعداد/تاریخ/ارز | قالب‌بندی محلی اعمال نشده |
| USSD/SMS/IVR | en/fa/ar پشتیبانی می شود (محدودیت ۱۶۰/۱۸۲ کاراکتر در نظر گرفته شده) |

### ۱.۵ شکافها و ریسکهای کلیدی

| # | شکاف | اثر | راه‌حل در این سند |
|---|---|---|---|
| ۱ | حذف `frontend/` از main و پراکندگی سه نسل | عدم قطعیت «کدام نسخه مرجع است» | فاز ۰: پذیرش نسل ۲ به‌عنوان مبنا + مهاجرت به پشتهٔ واحد (بخش ۳) |
| ۲ | i18n سه‌گانه و بدون مسیر زبانی | سئو، اشتراک‌گذاری، جهانی‌سازی شکست میخورد | معماری i18n بخش ۰ + next-intl + middleware |
| ۳ | فهرست زبانهای سند (۱۲) ≠ کد (۱۴) | سرگردانی تیم | تثبیت فهرست رسمی ۱۴ زبان در بخش ۰ |
| ۴ | RTL فقط با toggle | آینه‌سازی ناقص، باگهای bidi | CSS منطقی کامل + craft رعایت bidi + تست RTL خودکار |
| ۵ | بدون نظام طراحی/توکن | ناسازگاری بصری در ۹۸۵ صفحه | بخش ۴: توکن DTCG + قالبهای صفحه |
| ۶ | ۱۱۰ صفحهٔ مدل علمی تک‌الگو | خستگی کاربر، تکرار | آرکتایپ «ابزار علمی» بخش ۶٫۳ |
| ۷ | عملکرد سنجیدهنشده | ریسک Core Web Vitals | بودجهٔ عملکرد + Lighthouse CI در فاز ۰ |
| ۸ | دادهٔ جعلی احتمالی در دکور | نقض اصل صداقت | «مُهر منبع» اجباری روی هر عدد در UI |

---

## بخش ۲ — مرزهای تغییر (پیش از هر بازطراحی)

**۱) آنچه باید حفظ شود (غیرقابل تغییر):**
قواعد دهگانهٔ تجارت (بخش ۷)، محتوای بیانیه‌ها و نقل‌قول کاور، اصل «دادهٔ واقعی یا برچسب صریح»، نقشها و سلسله‌مراتب نظارتی، معماری محتوای تایپ‌شدهٔ fa/en، فهرست ۶۲ ابزار علمی و نگاشت آن به سرویسها، نشان برند (برگ + مدار ماهواره)، دامنه و سئوی موجود.

**۲) آنچه قابل بازسازماندهی است:**
ناوبری و IA پنج بافت، چیدمان و زبان بصری همهٔ صفحات، معماری فنی فرانتاند (پشتهٔ واحد)، سیستم i18n و مسیریابی زبان، الگوهای کامپوننت، حرکت و انیمیشن، تراکم اطلاعاتی هر بافت.

**۳) منشأ اصلی «معمولی بودن» طراحی فعلی:**
صفحات عمومی از الگوی «هیرو + کارت‌های متقارن + CTA» پیروی میکنند و داشبورد علمی از کارت‌های سه‌بعدی تزئینی؛ نتیجه یکدست نبودن میان پنج بافت و نبود «زبان بصری مالکیت‌پذیر» است.

**۴) شدت تحول:**
`Full Redesign` لایهٔ بصری + `Layout Restructure` معماری اطلاعات، با حفظ کامل مرزهای بند ۱.

**۵) جهت طراحی واحد: «زمینِ زنده» (Living Terrain)**
> طراحی پلتفرم را مثل **نقشهٔ زندهٔ زمین** می‌سازیم برای جوامع روستایی، اقلیمی، نهادهای علمی و بازار جهانی؛ با زبان بصری «دقت نقشه‌کشی + گرما روایی»؛ کانون حافظه: **خطوط کانتور زنده و مُهر منبع روی هر عدد** — هر عدد پلتفرم یک نشان کوچک منبع/تاریخ دارد که با hover یا tap «مهر» باز میشود.

تفکیک ظاهر بافت ها با تراکم و لحن (نه با پالت جدا):
کاور و عمومی = روایی نفس‌کشیدن؛ بازارگاه = چگالی فروشگاهی کارا؛ علمی = میزکار داده‌محور با نقشهٔ دائمی؛ ادمین = تراکم بالا و بی‌طرفی؛ پژوهش = کتابخانه‌ای آرام.

---

## بخش ۳ — پشتهٔ فناوری فرانتاند ۲۰۲۶

### ۳.۱ معیار انتخاب
(۱) رندر سمت سرور برای سئوی ۱۴ زبانه؛ (۲) RTL و ICU درست؛ (۳) مقیاس ۹۸۵ صفحه با جزیره‌های تعاملی سبک؛ (۴) بلوپرینت ۲۰۲۶ پایدار نه آزمایشی؛ (۵) سازگاری با تیم پایتونی موجود (API-first + codegen).

### ۳.۲ معماری مرجع (جدول لایه به لایه)

| لایه | انتخاب ۲۰۲۶ | دلیل | جایگزین در صورت بازگشت |
|---|---|---|---|
| چارچوب | **Next.js (App Router، نسخهٔ 15/16) + React 19** | RSC و Server Actions پایدار شدند؛ PPR برای ترکیب محتوای ایستا و پویا؛ Turbopack برای dev سریع؛ سئوی ۱۴زبانه آسان میشود | Astro 5 (اگر صفحات عمدتاً ایستا ماندند) |
| کامپوننت React | React Compiler + `use()` + Suspense streaming | حذف memoهای دستی؛ آبشار داده روان | — |
| زبان | TypeScript 5.9 strict + Zod 4 برای قرارداد داده | قرارداد واحد با OpenAPI بکاند | — |
| استایل | **Tailwind CSS 4** (کانفیگ CSS-محور `@theme`) + رنگ oklch + ویژگیهای منطقی RTL | توکنهای تم مستقیم در CSS؛ container queries بومی | vanilla-extract |
| CSS مدرن (Baseline 2025/26) | View Transitions، Anchor Positioning، `popover`، `:has()`، `subgrid`، `@layer`، تو در توی بومی، انیمیشن scroll-driven، `light-dark()`، `text-wrap: balance` | بدون JS اضافه؛ Interop 2026 پوشش میدهد | polyfill دگرگونیهای نما |
| kit رابط | Radix UI primitives + سیستم طراحی اختصاصی «زمینِ زنده» (نه قالب آماده) | دسترسپذیری آماده + هویت مستقل | Ark UI |
| i18n | **next-intl** (ICU MessageFormat) + middleware مذاکره + پوشهٔ `messages/<locale>/` | مسیر پیشوندی، fallback، سئو | react-i18next |
| داده | RSC fetch + TanStack Query 5 برای کلاینت + **Orval** (تولید کلاینت از OpenAPI) | ۵۷ سرویس بکاند → تایپ امن خودکار |openapi-typescript |
| نقشه و دادهٔ مکانی | MapLibre GL JS 5 + deck.gl 9؛ گوتنک PMTiles برای کاشی سبک | مرزکشی پروفایل زمین، نقشهٔ بازارچهها | Leaflet (کم‌حجم برای USSD-web ساده) |
| سه‌بعدی/ویزوال سنگین | Three.js با React Three Fiber؛ هدف WebGPU (TSL) با سقوط به WebGL2 | شبیهساز و برش خاک؛ بار تنبل | — |
| نمودار | ECharts (میزی/سریزمانی) + recharts برای نمونههای سبک | داده علمی متراکم | visx |
| حرکت | Motion (جانشین framer-motion) + انیمیشن CSS scroll-driven | کنترل حرکت معنادار؛ احترام به reduced-motion | — |
| آفلاین/PWA | **Serwist** + IndexedDB/OPFS + **Yjs CRDT** (هم‌راستا با معماری local-first موجود `docs/architecture/local_first/`) | کشاورز میدانی اینترنت ناپایدار دارد؛ فرمها و نتایج آخرین اجرا آفلاین میمانند | workbox |
| محاسبهٔ آفلاین | **WebAssembly (هستهٔ Rust/پورت از C++)** برای تقریب محاسبات سبک در مرورگر | اجرای محاسبهگر ساده بدون شبکه | محاسبه سمت سرور فقط |
| فرم | react-hook-form + Zod + Server Actions | اعتبارسنجی مشترک کلاینت/سرور | — |
| وضعیت UI | Zustand (جزیرهای، سبک) | سبد، تب فعال، فیلترها | Jotai |
| تست | Vitest 4 + Testing Library + MSW 2 + **Playwright** (شامل پروژههای per-locale برای fa/ur/ar و بررسی bidi) + Storybook 9 + رگرسیون بصری (Loki/Chromatic) + axe-core | پوشش ۹۸۵ صفحه فقط با تست الگو ممکن است | — |
| ابزار build | pnpm 10 + Turborepo 2 + Biome 2 (lint+format) + Style Dictionary 4 (تولید توکن) | یکپارچگی مونوریپو | eslint+prettier |
| پایش | Sentry + OpenTelemetry Web + RUM Core Web Vitals + Lighthouse CI در دروازهٔ CI | دید عملکرد واقعی کاربر میدانی | — |
| استقرار | ISR برای صفحات محتوایی؛ Edge middleware برای مذاکرهٔ زبان؛ docker خودمیزبان (سازگار با render/railway موجود) | هیچ قفل ابری | — |

### ۳.۳ بودجهٔ عملکرد (اجباری، قابل اندازهگیری)

| متر | سقف | روش پایش |
|---|---|---|
| JS اولیهٔ صفحات عمومی | ≤ 200KB gzip | bundle-analyzer در CI |
| LCP / INP / CLS (p75 موبایل) | ≤2.5s / ≤200ms / ≤0.1 | RUM + Lighthouse CI |
| فونت | ≤ 3 خانوادهٔ متغیر، زیرمجموعهٔ unicode-range، `font-display: swap` | گزارش شبکه |
| تصویر | AVIF/WebP خودکار، `sizes` درست، LCP بدون تنبلی | CI audit |
| نقشه/سه‌بعدی | فقط جزیره؛ بارگذاری با interaction یا viewport | قالبهای بخش ۶ |

### ۳.۴ مسیر مهاجرت (سه نسل → یک پشته)

۱) بازیابی `frontend/` نسل ۲ به main (برنچ `restore/frontend-gen2`)؛ ۲) استخراج داراییها (محتوا، فونت، تست، sitemap) به پکیجهای `packages/*`؛ ۳) ساخت شل جدید Next.js در `apps/web` با توکنها و kit؛ ۴) انتقال صفحه به صفحه با معیار «هیچ رگرسیون بصری بدون تصمیم آگاهانه»؛ ۵) حذف رسمی نسل های دیگر از اسکریپت های build.

---

## بخش ۴ — نظام طراحی «زمینِ زنده»

### ۴.۱ توکنها (W3C DTCG → Style Dictionary → متغیرهای CSS)

| خانواده | محتوا |
|---|---|
| رنگ (oklch) | آب `oklch(0.62 0.11 235)`؛ سبز پوشش `oklch(0.55 0.12 150)`؛ خاک `oklch(0.45 0.06 70)`؛ کاغذ/زمینهٔ روشن `oklch(0.97 0.005 90)`؛ مرکب `oklch(0.24 0.01 250)`؛ کنش اصلی = سبز پوشش؛ کنش هشدار/پول = مسّی آجری `oklch(0.60 0.14 45)`؛ پالت دادهٔ کورکور-امن ۸تایی برای نمودار |
| تمها | روشن، تیره (بازطراحی دقیق نه invert)، کنتراست-بالا، چاپ |
| تایپ | مقیاس ۱٫۲۵ (منحنی ۱۲ پله)؛ ارتفاع خط فارسی ≥1.7؛ فاصلهگذاری اعداد جدولی |
| فضا | شبکهٔ 4px؛ ظرف 1280px؛ تراکم سهگانه: `cozy` (عمومی) / `compact` (بازارگاه/ادمین) / `dense` (جدول علمی) |
| شعاع/سایه/لبه | شعاع 3 پله (2/8/16)؛ سایهٔ نقشه‌ای نرم دوپله؛ خط کانتور 1px به‌عنوان موتیف برند |
| حرکت | مدت 120/240/480ms؛ منحنی `ease-out-quart`؛ کنشهای صحنهای با انیمیشن scroll-driven؛ همه با `prefers-reduced-motion` خاموششونده |
| تراکم داده | «مُهر منبع»: نشان ۱۴px منبع/تاریخ روی هر عدد؛ بازشدن tooltip با `popover` |

### ۴.۲ تایپوگرافی چنداسکریپتی

| نقش | fa/ar/ur | لاتین/سیریلیک | hi/bn | zh |
|---|---|---|---|---|
| نمایش (تیتر) | Markazi Text (سریف تحریری) | Markazi Text لاتین | Noto Sans Devanagari SemiBold | Noto Sans SC Medium |
| رابط/متن | Vazirmatn متغیر | Vazirmatn لاتین | Noto Sans Devanagari | Noto Sans SC |
| داده/عدد/کد | JetBrains Mono (همیشه جهت LTR با `unicode-bidi: isolate`) | ″ | ″ | ″ |

هر خانواده باندل محلی + زیرمجموعه؛ ارتفاع خط per-script با متغیر `--lh-script`؛ اردو برای تیترها Noto Nastaliq با `line-height` بالاتر.

### ۴.۳ فهرست مولفههای پایه (حدود ۵۰)

هسته: Button، IconButton، Input/Textarea/Select (تک و جستوجوپذیر)، DatePicker شمسی+گریگوری، Checkbox/Radio/Switch، Tabs، Accordion، Dialog/Drawer (popover بومی)، Tooltip، Toast، Breadcrumb، Pagination، Table (چگال/فراخ)، DataGrid سبک (فیلتر/مرتبسازی/ستون‌بندی)، Card (سه تراکم)، Badge/Tag/Chip، Stat (با مُهر منبع)، Progress/Spinner/Skeleton، EmptyState، ErrorState، OfflineBanner، Stepper/Wizard، CommandPalette (`⌘K` با ذخیرهٔ کلید ترجمه)، LocaleSwitcher (۱۴ زبان با نماد جهت)، CurrencyUnitToggle (تومان/دلار)، NumberFlow (قالب محلی)، ProvenanceStamp، MapView (پایه)، ChartFrame (پایه)، MediaUploader، FileDrop، Sheet (موبایل)، SegmentedControl، Slider/Range، SearchFilter، EmptyRegionMapPlaceholder، PrintHeader.

### ۴.۴ دوازده قالب (آرکتایپ) صفحه

| کد | قالب | کاربرد نمونه | اسکلت |
|---|---|---|---|
| T01 | کاور | `/` | تمام‌صفحهٔ روایی، انتخابگر زبان، دو کنش، نوار وضعیت |
| T02 | محتوایی | درباره، بیانیهها، مقاله | ستون خوانش 72ch + فهرست لبه؛ چاپ تمیز |
| T03 | کشف/فهرست | دستهها، بازارچهها، مدلها | سرصفحهٔ فیلتر چسبان + شبکهٔ ناهمگون + نوار نتیجه |
| T04 | جزئیات | محصول، پروفایل زمین، مدل | ستون اصلی + ستون کنش چسبان؛ برگههای سند/نظر/تاریخ |
| T05 | جریان چندمرحلهای | پرداخت، تأسیس بازارچه، ساخت فروشگاه | Stepper خطی + ذخیرهٔ خودکار هر گام + خلاصهٔ کنار |
| T06 | داشبورد | داشبورد خریدار/فروشنده/پژوهشگر | ردیف شاخص + شبکهٔ ویجت قابل شخصی + خط زمان |
| T07 | ابزار علمی | ۶۲ ابزار + محاسبهگرها | پنل ورودی چسبان + صحنهٔ خروجی (نقشه/نمودار) + کشرای مستندات |
| T08 | جدول مدیریت | ادمین، انبار، مالی | فیلتر پیشرفته + جدول چگال + کشوی جزئیات + bulk |
| T09 | سند حقوقی | شرایط، قرارداد نسخه‌دار | شمارهگذاری سطر، diff نسخهها، رضایت ثبت |
| T10 | گزارش/چاپ | گزارش اثر، فاکتور | نسخهٔ چاپی اختصاصی + خروجی PDF سمت سرور |
| T11 | سامانهای | 404/503/آفلاین/وضعیت | تک‌ستون آرام، راهنمای بازگشت |
| T12 | آموزشی | راهنماها، USSD/صوتی | دو ستونهٔ گام-به-گام + رسانه + واژه‌نامه |

### ۴.۵ حالت اجباری هر صفحه (State Coverage)

برای هر صفحه و هر بلوک داده: `loading` (اسکلت شکل‌درست)، `empty` (با گام بعدی واقعی، نه جملهٔ خالی)، `error` (بازآوری + کد پشتیبانی)، `partial` (دادهٔ ناقص با مُهر «ناقص»)، `offline` (نوار + امکان ادامهٔ محلی)، `degraded` (تخریب اعلامشدهٔ سرویس). حالتها ترجمهپذیر و در Storybook مستقل‌اند.

### ۴.۶ دسترسپذیری و RTL (اجباری)

- WCAG 2.2 AA: کنتراست ≥4.5:1، هدف لمسی ≥44px، ناوبری کامل کیبورد، `aria-live` برای دادههای زنده، label همهٔ فرمها.
- RTL: فقط ویژگیهای منطقی (`padding-inline`, `margin-inline-start`, `inset-inline`)؛ آینهسازی آگاهانه (نمودار محور زمان LTR میماند؛ آیکون های جهتدار آینه می شوند؛ اعداد/کد isolate).
- تست RTL خودکار: پروژهٔ Playwright مجزا برای `fa`، `ur`، `ar` با اسکرینشات مقایس های.

### ۴.۷ حاکمیت سیستم طراحی

Storybook 9 به‌عنوان مرجع زنده + رگرسیون بصری در CI + نسخه گذاری پکیج `packages/ui` (semver) + ADR برای هر تغییر شکسته + «صفحهٔ نمونهٔ زنده» در هر بافت (آدرس `/fa/–/design-system` فقط برای تیم).

---

## بخش ۵ — معماری صفحات و فهرست به روز

### ۵.۱ نقشهٔ بافت ها و مسیرها

| بافت | مسیر پایه | ناوبری اصلی |
|---|---|---|
| کاور | `/` (پس از انتخاب زبان → `/{locale}/`) | دو کنش + زبان |
| عمومی | `/{locale}/public/**` (مسیر بدون پیشوند public هم معتبر) | سرصفحهٔ عمومی |
| بازارگاه | `/{locale}/market/**` | نوار بازارگاه با سبد و جستوجو |
| علمی هیدروما | `/{locale}/hydroma/**` | سایدبار هوشمند جمع‌شونده + فرمان کیبورد |
| ادمین | `/{locale}/admin/**` | ریل فرمانی + مهدور |
| پژوهش | `/{locale}/research/**` | میزکار دوپنلی |
| سامانهای | `/status`، `/{locale}/system/**` | — |

### ۵.۲ جدول شمارش نسخهٔ ۲ (917 → 985)

| بافت | نسخهٔ ۱ | افزودنی نسخهٔ ۲ | نسخهٔ ۲ |
|---|---|---|---|
| کاور | ۱ | بازطراحی T01 | ۱ |
| عمومی | ۴۸ | +۲۲ (هوش مصنوعی ۸، اعتماد و منشأ ۶، توسعه دهندگان ۸) | ۷۰ |
| بازارگاه | ۴۳۰ | +۱۴ (کیف پول/مالی/بیمه/داوری پیشرفته) | ۴۴۴ |
| علمی هیدروما | ۱۶۴ | +۱۰ (دستیار علمی، مطب نقشه، آفلاین، MRV) | ۱۷۴ |
| ادمین | ۱۲۴ | +۱۰ (پرچم ویژگی، مونیتور event_bus/jobs، چندمستاجری، توکن طراحی) | ۱۳۴ |
| پژوهش | ۶۶ | +۶ (DOI، پیش‌چاپ، یادگیری فدرال) | ۷۲ |
| سامانهای | ۲۴ | +۶ (PWA update، آفلاین، سقوط WebGPU، locale-fallback) | ۳۰ |
| دستیار زمینه‌ای | ۲۰ | — | ۲۰ |
| قالبهای پیام | ۴۰ | +۳ (واتساپ، آنتروپی پیام محلی، الگوی USSD دوزبانه) | ۴۳ |
| **جمع** | **917** | **+68** | **985** |

### ۵.۳ صفحات جدید نسخهٔ ۲ (۶۸ صفحه، به تفکیک کانون)

**کانون هوش مصنوعی (۸ — عمومی):**
`/ai` هاب دستیار (معرفی + مرزهای مسئولیت) · `/ai/assistant` چت با دستیار (RAG روی دانشbase + نقل منبع) · `/ai/voice` نسخهٔ صوتی چهارزبانه (fa/en/ar/ur) · `/ai/agents` معرفی عاملهای تخصصی (آبیاری/کود/کربن) · `/ai/glossary` واژه‌نامهٔ هوشمند · `/ai/ethics` سیاست هوش مصنوعی مسئول · `/ai/limits` محدودیتها و خطاها · `/ai/feedback` حلقهٔ بازخورد انسانی.

**کانون اعتماد و منشأ داده (۶ — عمومی):**
`/trust/provenance` کاوشگر منشأ هر داده (پیوند به سرویس provenance) · `/trust/carbon-registry` کاوشگر رجیستری کربن روی بلاکچین (آدرس/tx قابل بازدید) · `/trust/audits` گواهیها و بازرسیها · `/trust/disclosure` افشای مسئولانهٔ آسیبپذیری (فورم امن) · `/trust/sanctions` سیاست تحریم و تطبیق · `/trust/report` گزارش سالانهٔ شفافیت.

**کانون توسعهدهندگان (۸):**
`/developers/api` پورتال API با مرورگر OpenAPI زنده · `/developers/sdks` SDKهای TS/Python + نمونهها · `/developers/webhooks` فهرست رویدادها و امضا · `/developers/playground` زمین بازی احرازشده · `/developers/changelog` تغییرات API نسخهدار · `/developers/status-api` سلامت و سهمیه · `/developers/cookbooks` دستورالعملهای یکپارچهسازی · `/developers/partners` همکاری فنی.

**کانون کیف پول و مالی (۱۴ — بازارگاه):**
هاب EcoWallet (`/wallet`) · تاریخچه و صورتحساب · برداشت و تسویه · کارمزد و شفافیت · escrow center (امانیت به تفکیک سفارش) · درخواست و پیگیری تسهیلات خرد (ماژول ۱۵ نقشهٔ راه) · امتیاز اعتماد اعتباری کشاورز · بیمهٔ شاخص اقلیمی (ماژول ۱۶) · صفحهٔ رخداد خسارت و محرک پرداخت قرارداد هوشمند · بیمهنامههای من · داوری پیشرفته (اتاق اختلاف با پیوست مدرک) · مرجوعیهای چندمرحلهای · صورتحساب کمیسیون · مرکز مالیات.

**کانون آفلاین و شمول (۶ — سامانهای/عمومی):**
`/offline` تجربهٔ آفلاین (چه کارها آفلاین ممکن است) · `/system/pwa-update` راهنمای بهروزرسانی · `/system/webgpu-fallback` سقوط مرورگر قدیمی · `/accessibility` بیانیهٔ دسترسپذیری · `/simple` نسخهٔ سبک (تصویر-کم، 14 زبان، مناسب شبکهٔ ضعیف) · `/telecom` هاب USSD/SMS/IVR (راهنماهای موجود یکی میشوند).

**کانون منطقه‌ای (۶ — عمومی):**
صفحات فرود پایلوت ایران/پاکستان/افغانستان/ترکیه/منطقهٔ خاورمیانه/جهانی — هر صفحه: دستاوردهای محلی با مُهر منبع، شرکای محلی، شمارهٔ تماس و زبانهای فعال.

**علمی (۱۰):** دستیار علمی درونمیزی · مطب نقشه (Map Studio: لایهها/برش/مقایسه) · کارگاه عدمقطعیت · هاب MRV (چرخهٔ کامل شهری+IoT+ماهواره) · دفتر انتقال پذیری (reproducibility) · آفلاین کامپایل WASM ابزارهای سبک · مدیریت کالیبراسیون محلی · ناظر صف اجرا · ویرایشگر سناریوی مشارکتی (CRDT) · اتاق بازبینی مدل.

**ادمین (۱۰):** پرچم های ویژگی · مونیتور event_bus · صف jobs و بازپخش · مدیریت مستاجرها (Multi-Tenant ماژول ۱۷) · مدیریت توکنهای طراحی · مدیریت ترجمه (صف MT/انسانی، پوشش per-locale) · پایش ترجمههای گمشده · ابزار شبه‌محلی سازی · مدیریت رسانهٔ چندزبانه · داشبورد هوش مصنوعی مسئول.

**پژوهش (۶):** DOI و نقل‌قول دیتاست · سرور پیش‌چاپ داخلی · خط لولهٔ بازتولیدشونده · یادگیری فدرال بین پایلوت ها · ثبت اخلاق محلی · کتابخانهٔ الگوهای مطالعه.

---

## بخش ۶ — طراحی هر صفحه (به تفکیک بافت)

> برای هر گروه: هدف، قالب (T0x)، نکات طراحی کلیدی. صفحات شاخص شرح بخش‌به‌بخش دارند. همهٔ صفحه ها ۱۴زبانه، RTL/LTR، دارای حالت های §۴٫۵ و مُهر منبع روی اعداد.

### ۶.۰ کاور (T01 — بازطراحی عمیق)

ساختار نهایی:
1. **آسمان متن** — پسزمینهٔ تیرهٔ عمق آب تا خاک با بافت کانتور متحرک CSS (بدون JS)؛ عنوان «هیدروما نوژین» با Markazi Text درشت؛ زیرعنوان و شعار دقیقاً طبق متن مصوب.
2. **نقل‌قول** — بلاک نقل قول با مُهر «متن مصوب، تاریخ» (نه تصویر ساختگی).
3. **انتخابگر زبان** — کارت ۱۴ زبانه (موج فعال + موج آیندهٔ نیمهشفاف)؛ انتخاب → بازنویسی مسیر و ذخیرهٔ کوکی+localStorage؛ fallback با برچسب.
4. **دو کنش** — «ورود به صفحات عمومی» (لینک برجسته) و «ورود به بازارگاه» (دکمهٔ توپر کنش)؛ کلید Tab ترتیب درست؛ تمرکز مرئی.
5. **سه خط وضعیت** — شمار بهره بردار / بازارچهٔ فعال / استان و شهر hydroma-nojin-master-plan.md، هر یک با ProvenanceStamp؛ در نبود داده: «پس ازاجرا» صادقانه.
6. **پیوندهای حقوقی + نشان نسخه + نوار وضعیت سرویس** — پابرگ کاور.

قواعد: بدون carousel، بدون ویدئوی اتومات، بدون گرادیان بنفش؛ انیمیشن فقط کانتور و ورود تدریجی ۲ مرحلهای؛ LCP روی تیتر متنی (سبک و فوری).

### ۶.۱ بافت عمومی (۷۰ صفحه)

| گروه | قالب | نکات طراحی کلیدی |
|---|---|---|
| خانه و معرفی (۷) | T02 | خانه شرح پایین دارد؛ بقیهٔ صفحات: هر صفحه با «چه/برای چهکسی/شواهد/محدودیت/گام بعدی» پنجبخشی ثابت |
| اهداف و بیانیهها (۶) | T02+T09 | سند با شمارهٔ پاراگراف، نسخهٔ بندی، دکمهٔ «متن را با ترجمهٔ ماشینی ببین» |
| اجزای پلتفرم (۸) | T02+T03 | هر جزء: نمایهٔ دمو زنده (جزیرهٔ سبک)، وضعیت واقعی سرویس (پایش زنده)، پیوند به ورود |
| خدمات و راهکارها (۹) | T02 | صفحهٔ هر مخاطب: داستان کوتاه، سه قابلیت اصلی، CTA واقعی (پایلوت/آموزش) |
| علم و شواهد (۶) | T02+T03 | مراجع با DOI؛ «روش اعتبارسنجی» نمودار جریان؛ «محدودیتها» پررنگ (اعتمادسازی) |
| آموزش و محتوا (۶) | T12+T03 | کتابخانه با فیلتر زبان/سطح؛ ویدئو با زیرنویس fa/en؛ واژهنامهٔ دوزبانهٔ قابل جستوجو |
| سیاستها و حقوقی (۶) | T09 | نسخهٔ تاریخدار + diff؛ چاپ تمیز |
| هوش مصنوعی (۸ جدید) | T02+T07 | چت با نقل منبع لینکدار؛ پاسخهای بدون منبع برچسب «پیشنویس مدل» |
| اعتماد و منشأ (۶ جدید) | T02+T03 | کاوشگر منشأ: هر داده → سرویس → تصویر داده → تاریخ |
| توسعهدهندگان (۸ جدید) | T02+T08 | مرورگر OpenAPI زنده با «امتحان کن» احرازشده؛ dark code theme JetBrains Mono |

**صفحهٔ شاخص — خانهٔ عمومی (`/{locale}/`):**
هیرو روایی (نه هیرو بنر عمومی): سه لایهٔ «آب/خاک/زندگی» با کانتور؛ سپس نوار وضعیت واقعی (سه عدد با مُهر)؛ «چرا اکو نوژین» به‌صورت جدول مسئله→پاسخ (نه کارت متقارن)؛ زنجیرهٔ علمی پیشنمایش تعاملی (۴ گام)؛ شمارش مدلها با لینک به بافت علمی؛ کانالهای دسترسی ۵گانه (وب/PWA/USSD/SMS/صوت) به‌صورت نوار افقی کشفپذیر؛ باند کربن با پیوند رجیستری؛ CTA واقعی. حداقل اسکریپت؛ همهٔ بخشها جزیرهٔ مستقل.

### ۶.۲ بافت بازارگاه (۴۴۴ صفحه)

| گروه | قالب | نکات طراحی کلیدی |
|---|---|---|
| کشف و مرور دستهها (۳۰) | T03 | درخت دستهٔ ۳سطحی با ریل فیلتر ثابت؛ فیلترهای سمنتیک (کمآببر/ارگانیک/محلی) به‌صورت «حالتهای نگاه» قابل ترکیب |
| جستوجو (۱۲) | T03 | جستوجوی عمومی با پیشنهاد زندهٔ چندزبانه؛ جستوجوی بصری (آپلود تصویر آفت) با progress صادقانه |
| صفحهٔ محصول (۲۴) | T04 | گالری لیس-بدون-jank؛ بلاک Buy-Box چندفروشنده؛ بلاک «اثر زیستی» با مُهر منبع؛ نسخهٔ عمده/خرده با Toggle واحد |
| مقایسه و ارزیابی (۱۴) | T03+T04 | جدول مقایسهٔ چسبان تا ۴ کالا؛ تاریخچهٔ قیمت سریزمانی؛ هشدار تقلب پررنگ |
| خرید و پرداخت (۲۶) | T05 | سه گام: سبد → پرداخت امانی → تأیید؛ قرارداد نسخه‌دار پیش از کنش مالی با چک‌باکس ثبت-مهر؛ کلید یکتا نشان داده میشود؛ کیف پول EcoWallet جایگزین ورود کارت |
| سفارش و پس از فروش (۲۴) | T06+T08 | خط زمان سفارش با رخدادهای واقعی سرویس logistics؛ داوری با پیوست مدرک |
| اعتماد و امنیت خرید (۱۰) | T02+T03 | کارنامهٔ فروشنده با سنجههای واقعی و بازهٔ اعتماد |
| بازارچهها (۴۲) | T03+T04+T05 | تأسیس بازارچه: ۱۰ گام با امضای دیجیتال ۵ مؤسس و ویزارد ذخیرهٔ خودکار؛ نقشهٔ بازارچهها MapLibre با کاشی سبک |
| ایجاد فروشگاه (۳۸) | T05 | انتخاب بازارچهٔ مادر اجباری در گام ۱ (قاعدهٔ «فروشگاه بدون بازارچه ندارد») |
| انبارداری (۵۵) | T08 | جدولهای چگال؛ شمارش دوره‌ای با فرم تلفن-سازگار؛ بارکد/شناسهٔ جهانی |
| حسابداری/مالی/بانکی (۶۰) | T08+T10 | دفتر امانت «فقطافزودنی» با نمایش تغییرناپذیری؛ تسویهٔ خودکار با وضعیت صف؛ اسناد PDF |
| پشتیبان فروش (۳۵) | T06+T08 | داشبورد فروش با قیف و ازدسترفتهها؛ NPS با مُهر پیمایش |
| نظارت سهلایهای (۳۲) | T08 | تسری تعلیق به‌صورت نمودار زنجیرهٔ بینایی |
| مشترک (۲۸) | T05+T06 | حساب چندنقشی با «نقش فعال» واضح (context switcher) |
| کیف پول/مالی جدید (۱۴) | T04+T05+T08 | شرح بالا |

**صفحهٔ شاخص — صفحهٔ محصول:** نوار فراتر بالا (تأمین→انبار→ارسال→گواهی)؛ ستون اصلی: گالری+مشخصات+اثر زیستی+سؤالوپاسخ؛ ستون کنش چسبان: قیمت محلیشده، Buy-Box فروشندگان رتبهبندی شده، کنشهای افزودن/گفتوگو؛ زیر: مقایسهٔ مشابه، بررسیهای تأییدخرید با امتیاز تفکیکی؛ حالت «کالای پرمخاطره» (نهادهٔ شیمیایی) نشان هشدار و الزام مجوز.

### ۶.۳ بافت علمی هیدروما (۱۷۴ صفحه)

| گروه | قالب | نکات طراحی کلیدی |
|---|---|---|
| نمای کلی و پایش (۱۲) | T06 | داشبورد با خط زمان زندهٔ رخدادهای داده؛ نقشهٔ پروفایلها پیشفرض |
| پروفایل زمین (۱۴) | T04 | مرزکشی روی نقشه (polygon editor ساده)؛ کارت خاک/اقلیم/آب با منبع هر لایه |
| ۶۲ ابزار علمی (۶۲) | **T07** | یک آرکتایپ واحد: پنل ورودی چسبان (فرم با اعتبارسنجی)، صحنهٔ خروجی (نمودار/نقشه/جدول)، نوار «نسخهٔ مدل + کالیبراسیون + بازهٔ اطمینان» همیشه مرئی، کشرای «چگونه کار میکند» با مرجع؛ دکمهٔ «اجرای دوباره با همین ورودیها» (بازتولید) |
| کتابخانهٔ مدلها (۱۲) | T03+T04 | کارت مدل با وضعیت (پایدار/آزمایشی/تعلیق) و نسخه؛ سند مدل بهصورت صفحهٔ MDX |
| اجرا و سناریو (۱۴) | T07+T06 | مقایسهٔ سناریوها بهصورت diff جدولی؛ عدمقطعیت با نوار MC/LHS |
| توصیهگر (۱۲) | T07 | «دلیل توصیه» اجباری: وزن شاخصها و دلیل رد گزینهها شفاف |
| داده و اتصالها (۱۲) | T08 | وضعیت اعتبارنامهها؛ تاریخچهٔ دریافت؛ مستندات API پیوندی |
| آزمایشگاه (۱۰) | T07 | برش خاک سهبعدی (WebGPU→WebGL2→تصویر ایستا) |
| گزارشها (۱۰) | T10 | قالب گزارش با پیشنمایش چاپ؛ زمانبندی و اشتراک |
| حساب و تنظیمات (۶) | T06 | سهمیهٔ محاسبات با gauge؛ لاگ فعالیت |
| افزودنیهای ۱۰گانه (جدید) | T07/T06 | شرح در بخش ۵٫۳ |

### ۶.۴ بافت ادمین (۱۳۴ صفحه)

همهٔ صفحهها T08 با ریل فرمانی (`⌘K`) و مهدور (ورود دومرحلهای اجباری):
نمای کلان (۸): توپولوژی سرویسها بهصورت نقشهٔ زندهٔ event_bus؛ کاربران (۱۶): RBAC با جداسازی وظایف و بازبینی دوره‌ای؛ بازارگاه (۱۸): صف داوری نهایی با SLA مرئی؛ مالی (۱۴): تطبیق بانکی با diff دوطرفه؛ علم (۱۶): چرخهٔ انتشار مدل با دروازهٔ اعتبارسنجی؛ محتوا/چندزبانه (۱۴): **صف ترجمهٔ محلی با سه ستون کلید/fa/هدف + پوشش per-locale**؛ حکمرانی (۱۴): لاگ حسابرسی جستوجوپذیر؛ امنیت (۱۸): پایش نفوذ و چرخش کلید با تقویم؛ گزارش (۶): گزارش ساز ذخیره پذیر؛ افزودنی های ۱۰گانه (جدید).

### ۶.۵ بافت پژوهش (۷۲ صفحه)

میزکار دوپنلی (دفترچهٔ چپ، محیط اجرا راست)؛ دیتاستها با DOI و کنش «نقل کن»; پیشثبت آزمون با فرم قفلشدنی (هش ثبت)؛ خروجی ها قالب مقالهٔ استاندارد؛ اخلاق با وضعیت کمیته؛ افزودنی های ۶گانه.

### ۶.۶ صفحات سامانه ای و پیامها (۳۰+۲۰+۴۳)

قالب T11 آرام و بیدرمانی؛ وضعیت سرویس زنده؛ صفحهٔ آفلاین فهرست «چه امکاناتی فعلاً کار میکند»؛ قالبهای پیام (ایمیل/پیامک/واتساپ/USSD) به‌صورت کاتالوگ پیش نمایشی در ادمین؛ محدودیت کاراکتر USSD/SMS در ویرایشگر نشان داده می شود.

---

## بخش ۷ — قواعد تجاری و جریانها (تثبیت‌شده)

ده قاعدهٔ سخت نسخهٔ ۱ به قوت خود باقی است (فروشگاه بدون بازارچه ندارد؛ هیئت مؤسس ۵نفره؛ قرارداد نسخه‌دار پیش از مالی؛ کلید یکتا؛ امانیت پیشفرض؛ نظارت تسری‌دار؛ شفافیت اجباری؛ دفتر امانت تغییرناپذیر؛ دادهٔ شخصی حداقلی؛ بدون ادعای تأیید بیرونی).

**جریانهای اصلی (سرفصل):** تأسیس بازارچه (۱۰ گام) · ایجاد فروشگاه (۸ گام) · خرید و امانیت (۶ گام) · نظارت سهلایهای · علمی (پروفایل→پایش→مدل→عدم قطعیت→توصیه→گزارش) · پژوهشی (فرضیه→پیشثبت→اجر→انتشار) · **جدید:** آفلاین (صف محلی→همگام CRDT→حل تعارض با مهر زمانی) · اختلاف و داوری (گفتوگو→پیوست مدرک→رأی بازارچه→استیناف به بازارگاه) · MRV کربن (شهری+IoT+ماهواره→اعتبارسنجی→صدور→ثبت بلاکچین) · ترجمهٔ محتوا (کلید جدید→MT→صف بازبینی→انتشار per-locale).

---

## بخش ۸ — استانداردهای غیرکارکردی (۲۰۲۶)

| حوزه | استاندارد |
|---|---|
| چندزبانه | ۱۴ زبان رسمی؛ پیشوند مسیر؛ ICU؛ fallback زنجیرهای؛ hreflang؛ سئوی per-locale؛ شبه‌محلیسازی در CI |
| دسترسپذیری | WCAG 2.2 AA؛ تست خودکار axe + کیبورد-اسکرینر در Playwright |
| کارایی | بودجهٔ §۳٫۳؛ RUM واقعی؛ سقوط تدریجی بدون JS برای صفحات محتوایی |
| سئو | SSR/ISR؛ فرادادهٔ per-locale؛ نقشهٔ سایت چندزبانه؛ دادهٔ ساختاریافتهٔ محصول/مقاله |
| امنیت | CSP سختگیرانه؛ CSRF توکن برای کنشهای نشستی؛ rate-limit؛ کلید یکتای مالی؛ پایش رخداد |
| حاکمیت داده | نگهداشت مشخص؛ حق حذف؛ ناشناسسازی پژوهشی؛ منبع هر داده |
| شفافیت علمی | هر خروجی: نسخهٔ مدل، کالیبراسیون، بازهٔ عدمقطعیت، شناسهٔ اجرا |
| پایداری وب | رسانهٔ تنبل، فونت زیرمجموعه، میزبان سبز، بودجهٔ کربن صفحه (CO₂e/بازدید گزارش شود) |
| پایش پذیری | لاگ ساختاریافته؛ هشدار تخریب صریح؛ RUM |

---

## بخش ۹ — فازبندی اجرا

| فاز | خروجی | معیار پذیرش |
|---|---|---|
| ۰ — پایهٔ فنی | بازیابی frontend نسل ۲ به main · شل Next.js + توکنها + kit پایه · middleware زبان · پوشهٔ messages ۱۴ زبانه (fa/en کامل، ۱۲ زبان اسکلت) · CI (Biome/Vitest/Playwright/Lighthouse) | سوییچ زبان روی کاور مسیر را بازنویسی کند؛ بودجهٔ عملکرد سبز |
| ۱ — عمومی | ۷۰ صفحهٔ عمومی (۵۰ نسل ۲ منتقل‌شده + ۲۰ جدید) | هیچ صفحهٔ خالی؛ همهٔ اعداد با مُهر منبع |
| ۲ — بازارگاه هستهٔ خرید | ۱۴۰ + ۲۸ مشترک + ۱۴ مالی جدید | خرید کامل تا پس از فروش با امانیت و کلید یکتا |
| ۳ — بازارگاه نهاد | ۴۲ بازارچه + ۳۸ فروشگاه | تأسیس ۵نفرهٔ کامل با امضای دیجیتال |
| ۴ — پشتیبان فروشنده | ۵۵ انبار + ۶۰ مالی | چرخهٔ کالا→انبار→سفارش→فاکتور→تسویه |
| ۵ — نظارت سهلایهای | ۳۲ | تسری تعلیق کار کند |
| ۶ — علمی | ۱۷۴ (۶۲ ابزار با T07) | هر ابزار: اجرا، مرجع، بازتولید |
| ۷ — ادمین | ۱۳۴ | پوشش سرویس های حیاتی + صف ترجمهٔ فعال |
| ۸ — پژوهش | ۷۲ | چرخهٔ فرضیه→انتشار |
| ۹ — سامانهای/پیام/آفلاین | ۳۰+۲۰+۴۳ + PWA/Yjs | آفلاین: فرم ها و آخرین اجراها کار کنند |
| ۱۰ — جهانی سازی کامل | موج ۱ و ۲ زبان ها + سئوی per-locale | پوشش ترجمهٔ ≥۹۵٪ در fa/en، ≥۸۰٪ موج ۱ |

---

## بخش ۱۰ — پیشنیازهای شروع

| مورد | وضعیت | اقدام |
|---|---|---|
| فهرست ۱۴ زبان | تثبیت شد (بخش ۰) | تأیید مالک محصول |
| بازیابی frontend نسل ۲ | لازم | برنچ restore + بازبینی PR |
| دستهٔ محصولات ۳سطحی | جزئی موجود | کارگاه طبقهبندی |
| متنهای حقوقی | پیشنویس | بازبینی حقوقی نسخهٔ ۱۴زبانه |
| نگاشت ۶۲ ابزار→سرویس | موجود در رجیستری | بازبینی و قفل |
| درگاه پرداخت و مدل تسویه | ناقص | تصمیم پیش از فاز ۲ |
| کمیتهٔ اخلاق و پیشثبت | سند موجود | رسمی‌سازی پیش از فاز ۸ |

---

## بخش ۱۱ — تصمیمهای باز و ریسکها

| # | تصمیم باز | پیشنهاد پیشفرض | زمان تصمیم |
|---|---|---|---|
| ۱ | تکچارچوب Next.js vs دوجانبهٔ Astro+Next | تکچارچوب Next.js | پیش از فاز ۰ |
| ۲ | میزبانی ابری vs خودمیزبان | خودمیزبان docker با قابلیت مهاجرت | فاز ۰ |
| ۳ | مرجع اعداد صفحهٔ کاور تا پایلوت | «پس از پایلوت» صادقانه | فاز ۱ |
| ۴ | WASM آفلاین: کدام ابزارها | ۱۰ محاسبه گر سبک | فاز ۶ |
| ۵ | زبانهای موج ۲ (tr/ps) زمان راهاندازی | پس از دادهٔ تقاضا | پویا |

ریسک های اصلی: حجم ۹۸۵ صفحه (پاسخ: قالبها+تولید از registry)، ترجمهٔ انسانی پرهزینه (پاسخ: موجبندی+MT با بازبینی برای محتوای حیاتی)، پایداری تیم فرانتاند (پاسخ: پشتهٔ رایج و مستندسازی ADR).

---

## جمعبندی

این نسخه، برنامهٔ ۹۱۷صفحه ای را با شناخت واقعی کد موجود (موتور C++/پایتون، ۵۷ سرویس، سه نسل فرانتاند) به یک **نقشهٔ راه اجراپذیر ۹۸۵ صفحه ای** ارتقا میدهد: پشتهٔ واحد ۲۰۲۶ (Next.js/React 19/Tailwind 4)، نظام طراحی «زمینِ زنده» با ۱۲ قالب صفحه، معماری i18n با **۱۴ زبان رسمی** از روز اول، و فازبندی i18n-اول که جهانیسازی را از «افزودنی بعدی» به «پیشفرض معماری» تبدیل میکند. سه ستون حاکمیتی نسخهٔ ۱ — فروشگاه بدون بازارچه ندارد، بازارچه با هیئت ۵نفره، مالی با قرارداد و کلید یکتا — دست نخورده باقی می ماند.
