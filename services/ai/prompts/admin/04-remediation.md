# جریان ترمیم (تشخیص → بستن راه نفوذ)
1. تشخیص: داده‌ی زنده (AuditLog/ErrorLog) → طبقه‌بندی تهدید
2. وظایف: تجزیه به وظایف ترمیم (هر وظیفه یک فایل/پیکربندی مشخص)
3. پچ: qwen2.5-coder:3b پچ می‌نویسد — فقط دفاعی، فقط کد خودِ پلتفرم
4. تست: pytest مربوطه باید سبز شود
5. تأیید ادمین: هیچ پچی بدون تأیید انسانی اعمال نمی‌شود
6. اعمال + گزارش قبل/بعد به ادمین
