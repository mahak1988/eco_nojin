# فاز تکمیلی — تکمیل/حذف نهایی فایل‌های باقی‌مانده (دستور: کامل+ایمپورت یا حذف)

> تاریخ: 2026-08-27 · طبق دستور: هیچ فایلی بدون تعیین تکلیف نمی‌ماند — یا کامل و ایمپورت می‌شود یا حذف.

## ۱) تکمیل‌شده — ۱۲ صفحه یک‌خطی `pages/` (روت‌شده در App.tsx → کامل شدند)
| صفحه | تکمیل |
|---|---|
| RothCModel | توضیح + ۵ مخزن کربن + محرک‌ها + جایگاه در پلتفرم |
| SWATModel | توضیح + فرایندهای شبیه‌سازی + وضعیت صادقانه اجرا |
| WatershedModel | زنجیره هیدرولوژی HyDroMa + داده رایگان + کالیبراسیون |
| ModelsLibrary | فهرست ۸ مدل (AquaCrop…CMIP6) + سیاست رایگان |
| SystemStatus | **زنده**: وضعیت واقعی /health + لایه‌های امنیتی + لینک OGC/WaterML |
| Visualization3D | راهنما + ارجاع به Virtual Land Lab |
| LandProfiles | ابعاد پروفایل + منابع داده |
| DataManagement | جریان داده + حریم خصوصی (RLS) |
| HelpDocs | فهرست مستندهای ۴۷/۵۵/۵۶/۵۷ + توصیه AI |
| APIDocumentation | گروه‌بندی اندپوینت‌ها + لینک /docs و /redoc |
| CapabilityAssessment | ۵ ارزیابی + روش‌شناسی صادقانه |
| Support | ۴ راه حمایت + تقدیرنامه‌ها |

همه با توکن‌های طراحی موجود (سیستم زمردی) و JSX چندخطی واقعی بازنویسی شدند — دیگر یک‌خطی نیستند.

## ۲) حذف‌شده
- `services/api_gateway/routers/stubs.py` — ۳ اندپوینت ثبت‌نشده (wallet/scenarios)؛ wallet الان واقعی است و هیچ‌جا register نشده بود → حذف.
- `tests/unit/test_placeholder.py` — assert True بدون ارزش (۷۹ تست واقعی داریم).
- `testing_lab/integration_tests/test_e2e.py` — placeholder → **ارتقا به E2E واقعی** (۶ تست زنده: health، فایروال، کمپوست، OGC، بومگردی، WAF؛ با skip هوشمند وقتی سرور خاموش است).

## ۳) وضعیت نهایی
- بیلد: 3.66s سبز · pytest: ۷۹ پاس · E2E زنده: ۶ پاس · درخت پاک.

## ۴) ممیزی مجدد (خط مؤثر) — چیزی باقی نمانده؟
- `frontend/src`: ۱۶۴ فایل — همه تعیین تکلیف شدند (استاب‌ها حذف، صفحه‌ها کامل).
- بک‌اند: stubs.py حذف؛ روترهای خالی ussd/voice/sync قبلاً ارتقا یافتند.
- اگر مورد دیگری هست، همان معیار «خط مؤثر + خروجی کامل» اجرا می‌شود.
